# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repository is

**microSched** — a single-user personal task / note / calendar / tracker web app, **AI-first**, being built as a **clean rewrite** of an old desktop app (`VC_QuanLyThoiGian`). It is the "dự án trục" (spine project) of the owner's summer-2026 study plan; the parent planning/strategy workspace is at `../../hoc_he_2026` (read its `chien-luoc-he-2026.md` / `track_ai_eng_strategy.md` for the wider plan and the AI-engineering learning goals this project serves).

**Current state (2026-07-23): the app has real code and is running in production.** `backend/` (FastAPI + SQLModel + Alembic, Python ≥3.14 via `uv`, ruff + pytest, 5 test files, 1 migration), `frontend/` (React + Vite; scripts `dev` / `build` / `lint` / `preview`), one root `Dockerfile`, three workflows (`ci.yml`, `deploy.yml`, `cron.yml`), live at `microsched.fly.dev` on Neon PG18 with 14 tables. **Build/lint/test commands are real — read them from `backend/pyproject.toml`, `frontend/package.json` and `.github/workflows/` instead of assuming or inventing them.**

> **📝 Sửa 2026-07-23 — mâu thuẫn D1.** Chỗ này trước đây vẫn ghi *"design-complete, pre-code · no application code, package manifest, test suite, or git repo yet"* — **đúng lúc viết, sai kể từ 2026-07-21** khi 003–007 xong. Mâu thuẫn nằm **ngay trong cùng một đoạn văn** (cuối đoạn đã ghi *"003–007 all DONE"*) và sống 2 ngày, vì không ai đọc lại đầu đoạn khi thêm vào cuối đoạn. Bắt được nhờ `agent-tasks/harness-audit/02` đối soát memory hai harness. **Bài học phải nhớ: *current state* thì sửa tại chỗ; chỉ *quyết định* mới áp luật "thêm dated note, đừng viết đè" ở §Working conventions.** Lẫn hai loại đó chính là cách một file luật tự nói dối — và nó nói dối với mọi agent đọc nó đầu tiên.

**Lịch sử pha thiết kế** (giữ nguyên làm hồ sơ quyết định): Language, framework, architecture, and hosting are all decided (see `architecture-brief.md`); the **physical schema** (DDL, PK=UUIDv7, ORM=SQLModel, migrations, indexes, enum encoding, 3-tier AI logging) is decided too (see `schema-physical-brief.md`, 2026-07-19). The tracking-design session **closed 2026-07-19** (`tracking-brief.md`, all items final): A/B/C data split, VND-only money model, `tracker_group`, `subscription` entity, capture flow (one-tap + undo), dashboard behavior spec, medication reminder (discreet noti), and a full normalization review (§10, K1–K17) — **the schema is now fully locked across the project**. The **encryption-review session closed 2026-07-20** (`tracking-brief.md` §6, `schema-physical-brief.md` §7.1): whole-DB column-level verdict (app-level AES-GCM on tracker/subscription names, entry notes, money columns, and private note/task bodies; everything else stays plaintext), pgvector/FTS exclusion rule for encrypted columns, and backup-key placement (`db-and-data-model-brief.md` §6) — **this closes the last data-layer decision**. The **frontend + auth session closed 2026-07-20** (`frontend-brief.md`, `auth-brief.md`): UI stack locked (React 19 + TypeScript + Vite 8 + Tailwind v4/shadcn-ui + TanStack Query v5 + Dexie with a hand-rolled outbox — **no sync-engine**), auth locked (Google OAuth + env allowlist via Authlib, server-side `session` table, private unlock = separate Argon2id passphrase with per-session 15-min TTL), plus the **AI × private ruleset R1–R7** (AI follows the session's private gate; encrypted columns still never get embeddings/FTS — runtime search over decrypted private content instead; private-in-context forces zdr/no-train routing for the whole cascade). The only remaining design decisions are the **Bước-1 AI choices** (embedding provider per the retention bar, vector dimension, default LLM, hybrid retrieval details). **Dev-harness locked 2026-07-20** (`devops-brief.md` §7 + `cost-brief.md` §6): T1 = Claude Pro (specs, ADRs, final diff review, security-critical code; Fable left Pro 2026-07-20 → Opus 4.8/Sonnet 5), T2 = Codex on ChatGPT Plus (executes agent-tasks; Sol/Terra/Luna ladder — Sol only takes spec'd work), T3 = 2× Google AI Pro (**test execution only**, directed by T1/T2). Scaffold specs **`agent-tasks/003–007`** (003 backend → 004 frontend → 005 Docker/Fly → 006 Neon/Alembic DDL → 007 auth = walking skeleton with Google login) — **003–007 all DONE 2026-07-21** — real Google login on `microsched.fly.dev`, Neon PG18, 14 tables.

**📝 2026-07-22 — post-skeleton session.** Phase C order is now **008a (crypto) → 008b (CD) → 008 (task slice) → 009–012**; CD was slotted before the pattern-setting task on purpose (`devops-brief.md` §9). A **cost-observability track opened** (`cost-brief.md` §7) after a real incident: Fly's 30-second health check ran `SELECT 1`, which kept Neon permanently awake (6 CU-hrs/day against a 100 CU-hr free allowance → compute suspended mid-month ≈ 2026-08-06). Fixed by splitting `/api/healthz` (liveness, never touches the DB) from `/api/readyz` (readiness). **Class of bug worth remembering: neither file was wrong — `schema-physical-brief.md` §185 had warned "cron đừng ping DB quá dày" three days earlier — the gap was *between* two correct decisions in files that don't reference each other.** No test, log, or security review could see it. Fix verified the same day (endpoint went Idle, then woke on real use); Neon stays under **daily watch through 2026-07-25** (`cost-brief.md` §7.3). Free-tier quota **resets on the 20th monthly** (next 2026-08-20) — CU-hours/transfer reset, but **storage is a continuous limit that never resets**.

**📝 2026-07-22 (late) — `008b` DONE and verified; CD is live.** Merging into `develop` now builds, deploys to Fly, and runs a **mandatory smoke test that asserts the deployed git SHA**, not just `status: ok` (a partly-failed deploy leaves the old machine serving happily, so checking liveness alone reports success for a failure). First automatic deploy ran clean. Also landed: `APP_ENV` (`production` | `local`, `Literal`-typed, defaulting to `production`) as **the single answer to "where am I running"** — environment-dependent guards must ask `settings.is_production`, never infer it from a neighbouring setting; `CRON_TOKEN` + a daily heartbeat cron that **never touches the DB**; the two 007 polish items. Phase C order is now **008b ✅ → 008a → 008 → 009–012** (008a is unblocked: `ENCRYPTION_MASTER_KEY` exists). Still open: cron's first real run (scheduled 10:17 VN, 2026-07-23) and the Neon watch through 07-25. Agents may now drive **the owner's real Chrome profile** — rules in `AGENTS.md` §"Lái trình duyệt" and `devops-brief.md` §7.2; account names are deliberately **never** written into this repo.

**📝 2026-07-22 (end of day) — harness upgrade: Claude now *orchestrates* Codex instead of the owner relaying by hand** (`devops-brief.md` §7.3, and §8 reopened). Locked: the plugin [`openai/codex-plugin-cc`](https://github.com/openai/codex-plugin-cc) (a *client* — it calls the local `codex` binary and its config/auth; **review gate deliberately off**); a **receipt rule** — Claude never accepts prose as evidence, only PR number + green `gh pr checks` + a readable diff (this is the 008b smoke-test lesson generalized); and a **triage ladder L1/L2/L3** that lets Claude decide what reaches the owner, keyed on **blast radius**, with the safeguard that **every L2 decision must appear in the PR description** — no decision disappears, it only moves to where the owner reads it. On **008** (the pattern-setting slice) L2 is promoted to L1. **Memory does not cross between harnesses** — Claude reads `~/.claude/`, Codex reads `~/.codex/`; required rules therefore live in `AGENTS.md` (repo law) and `~/.codex/AGENTS.md` (cross-project habits, written this session), while each side's auto-memory stays a recall layer only. Dev/test moves **off Neon onto Postgres in Docker** (never the host instance — it holds `microschedule_v2`), which removes §8's first hard limit; the remaining ones are bundled into fixed **lane slots** (worktree path + app port + PG port + OAuth redirect URI + Codex trust entry). Two things still to verify, not assume: whether the plugin path injects Codex's memories (`agent-tasks/harness-audit/01`, run twice and diff), and Jules as a lane (5-task measured trial at 008/009). New folders: `agent-tasks/harness-audit/` (specs) and `agent-tasks/harness-reports/` (**gitignored except its README** — the reports describe personal working habits, which gitleaks does not catch).

**📝 2026-07-23 — harness audit 01 + 02 xong; ba thứ đo được, không suy đoán.** ① **Đường plugin KHÔNG làm mất trí nhớ của Codex** — chạy cùng một prompt ở Codex app (A) và qua plugin (B) rồi lấy hiệu: bản B còn dán ra *nội dung* `MEMORY_SUMMARY` thật. Nhưng đọc cho đúng phạm vi: nó nói memory **của Codex** sống sót qua plugin, **không** nói memory đi được Claude→Codex — luật "memory không băng qua hai harness" giữ nguyên. ② **cwd của Codex TRÙNG cwd của Claude** ⇒ nhánh "worktree riêng mỗi luồng" **không mở**, và luật *"chạy nền ⇒ Claude không chạm cây làm việc"* **thành bắt buộc** (`devops-brief.md` §8). ③ Bản kê tool qua plugin là **tập con thật sự** (56/78; mất `codex_app__*` nên Codex không tự quản thread), **nhưng không thiếu thứ nào để thi công**; quyền ghi là **cờ `--write`**, không phải giới hạn cứng — phiên B ra "không ghi được" vì **chính prompt tự khai là phiên chỉ-đọc**, tức đo cái prompt chứ không đo đường plugin. **Hai lớp lỗi rút ra, cả hai đều im lặng:** (a) **chỉ thị tới nơi mà không được thi hành** — prompt bắt mở đầu bằng một dòng cố định, Codex đọc được dòng đó nhưng hiểu thành lời khai của caller nên không in ra; ⇒ *nghiệm thu phải kiểm dòng chữ thật trong sản phẩm, không suy từ "prompt đã gửi đúng"*. (b) **luật neo vào đường dẫn cũ** — `.gitignore` ghi `harness-reports/*`, mẫu có `/` ở giữa nên git **neo vào gốc repo**; thư mục dời sang `agent-tasks/` là mẫu hết khớp và báo cáo thành *untracked-nhưng-không-ignored* trên một repo public, gitleaks không chặn vì đây không phải secret. Vá bằng `**/harness-reports/*`; ⇒ *mỗi lần dời thư mục, đi soi mọi luật neo path (gitignore, CI path filter, CODEOWNERS, pre-commit `files:`)* — chúng không báo lỗi khi hết phủ. **Từ 02 còn một luật đọc-hiểu:** `~/.codex/memories/rollout_summaries/` là **biên lai của một thời điểm**, không phải current state — hai trong ba "mâu thuẫn" mà 02 nêu (PR #13, task 01 thiếu bản B) hoá ra chỉ là memory Codex đóng băng lúc phiên kết thúc, không có đường học chuyện xảy ra sau.

**Repo & workflow (see `devops-brief.md`):** GitHub repo is **public by deliberate choice**; work happens on `develop` → PR into `main` (ruleset `protect-main` blocks force-push/deletion and requires a PR; approvals are set to 0 on purpose — solo project; 5 required checks incl. `Production dependency check`). **Merging into `develop` deploys to production** (locked 2026-07-22 late, `devops-brief.md` §2.1 + §9 — **this reverses an earlier same-day decision that made `main` the trigger**; the two conditions were circular). `develop` = what is running on Fly, and it is where the owner and T3 verify. **`main` never deploys** — it is the *stable-release label*, applied with a **tag `v0.x`** when a slice is worth returning to. Say plainly what that changed: `main` no longer adds a verification layer (verification now happens continuously on `develop`); its remaining value is **a deliberately chosen rollback point**, and it has **no self-enforcing mechanism** — the only brake is the release-lag line the CD workflow prints after every deploy. Rollback = **roll-forward** (`git revert` on `develop`); the `workflow_dispatch` side-path only becomes usable once a workflow file reaches the default branch. Convention: **one commit per decision session**, Vietnamese message explaining *why*. Delegated work goes in `agent-tasks/NNN-<slug>.md` as self-contained specs; from 003 on, **code** tasks run on a `feat/NNN-<slug>` branch with a small PR into `develop` for T1 diff review (docs still commit straight to `develop`). Data boundary for third-party tools (devops-brief §7): public code/docs = any tier may touch; real secrets and personal data never enter agent sessions.

## Read the decision records before proposing anything

`docs/` holds **self-contained decision briefs** (written to be read with zero conversation context). They encode locked decisions *and their reasons* — read them rather than re-deriving:

- `docs/db-and-data-model-brief.md` — DB, hosting, backup decisions.
- `docs/schema-v1-brief.md` — the data model (entities + relationships), locked at concept level.
- `docs/schema-physical-brief.md` — the **physical** schema (column types, PK=UUIDv7, enum=TEXT+CHECK, indexes, ORM=SQLModel, Alembic+QA, 3-tier AI logging), locked 2026-07-19 except tracker specifics.
- `docs/tracking-brief.md` — tracking feature design, **locked 2026-07-19**: A/B/C data types, VND-only money model, `tracker_group`, `subscription`, capture flow + dashboard spec, normalization review (§10), medication reminder (§12), private-mode supersede (§5), encryption-review **closed 2026-07-20** (§6 — column-level verdict, mechanism, pgvector/FTS exclusion).
- `docs/forward-spec.md` — feature backlog + the "viewability" and AI-first design principles.
- `docs/upgrade-notes-inbox.md` — verbatim raw capture of the owner's upgrade notes (provenance for `forward-spec.md`).
- `docs/architecture-brief.md` — language/framework (Python + FastAPI), modular-monolith architecture, hosting (Fly.io), AI tool-layer/MCP sequencing, repo layout. **Read this before proposing any stack/infra change.**
- `docs/frontend-brief.md` — frontend UI stack, **locked 2026-07-20**: React 19 + TypeScript + Vite 8 + Tailwind v4/shadcn-ui + TanStack Query v5 + Dexie/hand-rolled outbox (no sync-engine; TanStack DB noted as a two-way upgrade door); runtime model (Node exists only at build time — production runs one Python process, multi-stage Docker); owner-device (iPhone) PWA consequences (push needs Home-Screen install; med-reminder ✓-on-noti needs a 2-tap fallback); shadcn + npm supply-chain conventions.
- `docs/auth-brief.md` — auth, **locked 2026-07-20**: Google OAuth + env allowlist (Authlib, login-only OIDC — Google app stays in Testing mode), server-side `session` table (opaque-token cookie, hash stored, rolling 60–90-day TTL), private unlock (separate Argon2id passphrase, 15-min session TTL, account-flag option rejected), **AI × private ruleset R1–R7**, cron-endpoint bearer auth.
- `docs/cost-brief.md` — running operating-cost tally across all chosen services; carries its own re-check date (~3 months), separate from architecture-brief so pricing drift doesn't invalidate decisions.
- `docs/devops-brief.md` — repo visibility + **the owner's threat model** (social engineering, not casual readers), git/PR workflow, secret-scanning layers, auto-PR-review options (deferred until there's code), agent-task convention. Read before touching repo settings or CI.
- `docs/migration-mapping-brief.md` — old data → new schema; where the real data lives.
- `docs/v1-reference.md` — old-app domain logic worth porting (code-level; not strategy).
- `docs/learnings-applied.md` — running log of concepts learned and applied.
- `scripts/inventory_old_stores.py` — read-only inventory of the OLD stores (needs the old app's venv python + `PGPW` env var). Reusable for cutover verification.

## Locked architecture (don't relitigate — see docs for the reasoning)

- **One data store only: PostgreSQL + `pgvector`, hosted on Neon.** No SQLite, no parallel stores. The old app ran SQLite *and* Postgres simultaneously and lost track of which held the truth — that split-brain is the anti-pattern this project exists to avoid.
- **Data model:** see `schema-v1-brief.md`. Core entities: `task`/`task_item`, `note`/`note_item`, `calendar_source`/`calendar_event`, `tracker`/`entry` (unified health + finance logging), `app_setting`, `audit_log`. Principle: **markdown for prose/body fields, structured columns for anything queryable**; timestamps on every entity; store full text, truncate only at display.
- **AI features are sequenced by blast radius:** Bước 0 (foundation) → 1 (read-only assistant, hybrid retrieval) → 2 (narrow write tools + confirm + audit) → 3 (finance). Auto-mode = **cascade self-verify**, not a learned router. Instrument/log from day one.
- **Backend: Python + FastAPI** (locked 2026-07-19). Python chosen for AI/RAG/eval ecosystem depth + AI-eng career alignment (re-examined against TS full-stack; hosting is cost-neutral between them so it wasn't a factor). FastAPI over Litestar for the biggest ecosystem / best AI-coding-assistant support (maximize time-on-AI, minimize framework plumbing) + Pydantic v2 being the de-facto tool-schema standard. **Do NOT justify this by "the owner already knows it from FANG" — familiarity was deliberately excluded as a weight (summer goal is renewal, not reuse).** ORM = **SQLModel**, Alembic with QA gates, PK = UUIDv7, enums = TEXT+CHECK, 3-tier AI logging — all locked 2026-07-19 in `schema-physical-brief.md`.
- **Architecture: modular monolith** (one process — web/domain/retrieval/agent/jobs as in-process modules, not services). No Celery/Redis; scheduled/background work (embeds, calendar import, backup, medication reminders) runs via external cron (GitHub Actions) hitting an endpoint. **Hosting: Fly.io**, 1× shared-cpu-1x Machine always-on (no scale-to-zero — cold start is a hard no for this user), region `sin`; start at 256MB (~$2/mo), scale to 512MB only if it OOMs. **Frontend = static SPA/PWA** (no SSR → one runtime; FastAPI serves the built static files on the same origin/Machine). DB stays Neon; no Fly volumes, no Fly Managed Postgres. See `architecture-brief.md` for full reasoning + rejected alternatives (Oracle Always-Free, VN shared hosting, Render free, Hetzner).
- **Frontend: PWA, offline-first for capture** (writes land in IndexedDB instantly, sync later); stack locked 2026-07-20 (`frontend-brief.md`): React 19 + TypeScript + Vite 8 + Tailwind v4 + shadcn/ui + TanStack Query v5 + Dexie with a hand-rolled outbox — **no sync-engine** (single-user has no multi-client conflict problem; avoid extra moving parts). Node exists only at build time; production runs a single Python process.
- **Auth (locked 2026-07-20, `auth-brief.md`):** Google OAuth + env allowlist via Authlib (login-only OIDC); server-side `session` table (opaque token cookie, hash stored); private unlock = separate Argon2id passphrase opening the session for a 15-min TTL (a *display* gate — the encryption master key stays app-held). **AI follows the session's private gate (R1–R7):** locked ⇒ agent tools filter `is_private`; unlocked ⇒ full read (write comes with Bước 2); encrypted columns still never enter pgvector/FTS (private search = runtime scan over decrypted content); private-in-context ⇒ force zdr/no-train for the whole cascade and exclude retention-mandatory models; messages created while unlocked are flagged private; background/cron AI is public-only.
- **AI tool layer is Pydantic-typed and MCP-ready by construction**, but MCP protocol itself is deferred until there's a second consumer beyond the internal agent — don't wire an MCP server prematurely.

## Hard boundaries (do not cross)

- The old app at `C:\Users\os\Desktop\old_prj\VC_QuanLyThoiGian` is **reference only**. Its `main` branch (v1 desktop) and the real SQLite DB at `C:\Users\os\Desktop\Tools\VC_microSchedule_home\todo.db` are **do-not-touch** (they are the rollback path). Read old stores **read-only** only.
- **Migration source of truth = the local Postgres `microschedule_v2`** (live, edited daily). The old SQLite is dead — ignore it.
- The local `postgres` **superuser hosts many of the owner's other projects** — microSched must get its **own limited DB role**; never reuse that superuser for the app.
- Credentials live only in `.env`, never committed; the first commit's `.gitignore` must block `.env`.

## Working conventions

- Docs are **decision records**: self-contained, Vietnamese prose with English technical terms kept inline, status-flagged (`✅ CHỐT` / `⚠️ OPEN` / `DEFER`). When a decision changes, add a dated note — don't silently rewrite prior conclusions.
- **Role split:** the owner decides architecture/product and reviews; Claude executes. Present options at the **strategy/product level**, not as low-level backend claims — the owner does not retain backend detail of the old app.
- Where a decision in `docs/` conflicts with the parent strategy docs in `../../hoc_he_2026`, the newer decision here wins (those docs predate this project's design phase).
- **pre-commit + gitleaks are active** (`.pre-commit-config.yaml`, gốc repo): mọi `git commit` chạy qua hook vệ sinh cơ bản + gitleaks (chặn secret lọt vào commit trước cả khi push). Hook **không tự theo `git clone`** — sau khi clone máy mới, chạy `pip install pre-commit` rồi `pre-commit install` trong repo để kích hoạt lại.
