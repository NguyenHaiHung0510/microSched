# Learnings applied — microSched

> Nhật ký **kiến thức đã học → áp dụng thật** vào microSched. Ghi ngắn: *học gì · áp vào đâu · ref*. Dùng làm nguyên liệu weekly-notes / blog / phỏng vấn.

## 2026-07-18

### Quy tắc sao lưu 3-2-1 (và mở rộng 3-2-1-1-0)
- **Học:** 3 bản sao · 2 loại phương tiện · 1 offsite. Mở rộng: **1** bản immutable/air-gapped, **0** lỗi (verify/test-restore định kỳ). Encrypt mọi bản.
- **Áp vào microSched (đúng cỡ single-user):** live trên Neon + dump-laptop + Google Drive auto-sync = **3 copy / 2 nơi**. **Bỏ** immutable (lo doanh nghiệp). **Giữ verify** = test-restore vào Postgres tạm định kỳ (backup chưa restore = "Schrödinger"). **Mã hóa** dump (chứa note riêng tư). Dump ghi **file timestamp**, không ghi đè.
- **Insight cốt lõi:** *provider durability ≠ backup* — nó chỉ chống hỏng phần cứng của họ, không cứu khỏi lỡ tay / mất account / provider biến mất. Với app 1 người: đầu tư vào **backup + verify**, KHÔNG vào availability tier (five-nines vô nghĩa).
- **Ref:** [3-2-1 Backup Rule Explained (IBM Technology)](https://www.youtube.com/watch?v=WJzsX32qMJY)

### Vài khái niệm khác đã áp (giai đoạn thiết kế microSched, 18/07/2026)
- **Hybrid retrieval (structured + semantic):** Postgres + `pgvector`, nguyên tắc *"cấu trúc ở chỗ cần query, markdown ở chỗ viết văn xuôi"* → phục vụ Bước 1 AI đọc. (nguồn: AI Engineering / strategy doc)
- **Một nguồn sự thật (chống split-brain):** VC cũ dùng cả SQLite + Postgres → không rõ data ở đâu. Mới: **một store Neon duy nhất**, role DB riêng giới hạn quyền.
- **Right-size characterization:** không copy y hệt hành vi app cũ (đang redesign) → port logic hữu ích + lo migrate data, dồn công test/eval sang **tính năng AI** nơi nó đáng.
- **Rewrite big-bang + đường lùi:** giữ VC cũ (`main`) chạy tới khi tự tin cutover.

## 2026-07-19 — Nhóm 2: schema vật lý

- **3 tầng schema (khái niệm → logic → vật lý):** "chọn Postgres" mới là engine; *schema vật lý* mới là kiểu cột/khoá/index/DDL thật — và là **cửa một-chiều** sau khi có data. Áp: `schema-physical-brief.md`.
- **UUIDv7 (RFC 9562, native PG18):** khoá chính time-ordered → index nhỏ ~26%, đọc-theo-thứ-tự nhanh ~3× vs UUIDv4; sinh được ở client → khớp **offline-first**. Bài học: chọn khoá chính là chọn *cả cách sinh ID* (server vs client), không chỉ kiểu dữ liệu.
- **Enum = TEXT + CHECK, không native ENUM:** schema còn tiến hoá → tránh `ALTER TYPE` phiền; AI-tool đọc/ghi text dễ hơn.
- **Index có đánh đổi:** tăng đọc nhưng tốn storage + chậm ghi (mọi INSERT cập nhật mọi index) → single-user vài trăm dòng thì **không over-index**; chỉ FK + cột thời gian. (Học "index là gì" từ 0.)
- **Migration autogenerate hay sót** (đổi type, CHECK, rename→drop-mất-data) → dựng **hàng rào QA CI** (round-trip, drift-check, chặn drop ngầm, thử trên bản restore) = một mẩu CI/CD đáng học.
- **Log AI 3 tầng (obs/eval):** hội thoại (message) ≠ log đủ; cần *phong bì* trace (tool/token/cost/model/accept-reject) tách khỏi *raw blob* prompt-đã-ráp (đẩy off-DB). Tách để vừa eval được vừa không phình DB.
- **Đo thật > ước tính:** đưa số từ `n_live_tup` ( estimate) ra "2 note" — SAI; `COUNT(*)` thật = 49 note / 887 dòng. DB v2 ~1 năm = 10MB → Neon 0.5GB là non-issue cho nội dung; chỉ log AI là biến số. Bài học: dùng exact count, cẩn thận multi-schema + thống kê cũ.

## 2026-07-19 — Phiên thiết kế tracking (đang chạy)

- **Event vs state (sự kiện vs trạng thái):** "tracking" hoá ra là 3 thứ — nhật ký hành vi + sổ chi tiêu (đều là *sự kiện*, gộp được) vs subscription (*trạng thái có vòng đời*, hỏi về tương lai) → entity riêng. Nhét trạng thái vào bảng nhật ký là chỗ app chi tiêu hay gãy. Áp: `tracking-brief.md` §1.
- **Thiết kế tâm lý "đo khoảng cách, không đếm":** chủ ý không ghi số điếu — ghi số biến app thành hạn mức; ghi "có một lần" biến metric chính thành *đã bao lâu rồi* (chỉ tăng khi không làm gì). Yêu cầu sản phẩm ngược lên schema: value optional **theo tracker**, không theo entry.
- **Threat model quyết định mô hình bảo mật:** "đọc thấp ghi cao" (Bell-LaPadula) sinh cho multi-user có địch bên trong; single-user threat thật = người nhìn qua vai → write-up chỉ tạo "ghi mù" (bug), không tạo bảo mật. Chọn mô hình theo threat model, không theo độ "chuẩn sách".
- **Embedding leakage:** mã hoá cột gốc nhưng lưu vector embedding của nó = vẫn rò *nghĩa* (vector là biểu diễn có mất mát của nội dung). Câu hỏi mã hoá đúng phải là "cột nào vào pgvector, chấp nhận rò nghĩa không". Áp: scope phiên encryption-review, `tracking-brief.md` §6.
- **Đa tiền tệ — chuẩn hoá về 1 đơn vị tính:** mọi phép tính chỉ chạm cột VND; ngoại tệ lưu dạng số (`orig_amount`+`orig_currency`) nhưng chỉ để hiển thị; KHÔNG auto-FX (kéo theo nguồn tỷ giá + tỷ giá-tại-thời-điểm — over-eng cho 1 user).
- **Adjacency list + recursive CTE:** cây phân loại bằng `parent_id` tự trỏ; lá = nút bấm khi ghi (phân loại không thêm bước nhập); cộng dồn nhánh bằng recursive CTE — micro-giây ở quy mô nhỏ, giá thật là code phức tạp hơn. *(Cuối phiên: RÚT — nhu cầu thật chỉ 1 tầng nhóm → bảng `tracker_group` 2 tầng thắng cây ở mọi mặt. Bài học: soi ví dụ thật trước khi chọn cấu trúc tổng quát.)*

*(các mục dưới ghi lúc đóng phiên, cùng ngày)*
- **Composite FK làm cảnh sát nhất quán cho denorm cố ý:** `tracker.kind` phải khớp `group.kind` → `UNIQUE(id, kind)` trên cha + FK `(group_id, kind)` từ con: DB tự chặn lệch, 0 code app; group NULL → ràng buộc tự miễn (MATCH SIMPLE).
- **Chọn kiểu thời gian theo BẢN CHẤT khái niệm:** *thời điểm sự kiện* = `timestamptz` (B2) · *ngày lịch* (hết hạn sub) = `DATE` · *giờ lặp hằng ngày* (nhắc thuốc) = `TIME`. Ép tất cả thành timestamptz mới chính là nguồn bug lệch-một-ngày quanh múi giờ.

## 2026-07-20 — Phiên encryption-review (đóng)

- **App-level encryption vs `pgcrypto`:** `pgcrypto` mã hoá/giải mã ngay trong câu SQL → khoá phải đi qua kết nối tới server Neon để dùng được, tự phá mục đích "app giữ khoá, DB không bao giờ thấy plaintext lẫn khoá". Mã hoá **ở tầng app** (thư viện `cryptography`, AES-GCM) trước khi INSERT giữ đúng ranh giới tin cậy: Neon (và ai chiếm được connection string) chỉ thấy ciphertext, không có đường lấy khoá qua chính kênh đó. Bài học: "mã hoá cột DB" không tự động có nghĩa "mã hoá trong DB" — vị trí thực thi phép mã hoá quyết định ai phải được tin.
- **Đọc chính sách nhà cung cấp = một phần của threat model, không phải formality:** tra live Anthropic API docs phát hiện lớp "Covered Models" (Fable 5/Mythos 5) **bắt buộc lưu 30 ngày**, khác mặc định no-retention của API — nếu chọn model Bước 1 mà không tra mục này, privacy bar coi như tự vỡ ở khâu chọn model, không phải khâu mã hoá DB. Bài học: privacy bar phải chạy dọc **toàn bộ chuỗi** (DB → app → LLM provider), một khâu hở là hở hết.
- **`age` (keypair encryption) cho backup file-level:** không như GPG cần quản lý web-of-trust, `age` chỉ cần một cặp khoá; **public key an toàn để nằm thẳng trong repo** (mã hoá không cần secret) — chỉ *giải mã/restore* mới cần private key. Tách rời "quy trình chạy hằng ngày" (cron mã hoá, không giữ secret nào) khỏi "quy trình hiếm khi chạy" (restore, cần private key) giảm bề mặt lộ khoá đúng chỗ cần giảm nhất.
- **Rà theo "đường rò", không rà theo "độ nhạy cảm của dữ liệu":** thay vì hỏi "cột này nhạy cảm không", câu hỏi đúng là "lộ nguyên khối DB/dump thì cột này có thành nguyên liệu pretext không" — hai câu khác nhau: `tracker.name` không "nhạy cảm" theo nghĩa thông thường nhưng lộ gần hết ngữ cảnh; số tiền trần một mình gần như vô hại nếu tên đã mã hoá. Bài học: threat model cụ thể (ai đọc được gì, dùng để làm gì) quan trọng hơn nhãn "sensitive/not sensitive" chung chung.
- **Trạng thái suy ra được thì đừng lưu:** `subscription` không có cột `status` — suy từ `expires_on` + `canceled_at` → miễn nhiễm update-anomaly (đúng tinh thần 3NF).
- **Partial unique index cho soft-delete:** UNIQUE `lower(name)` `WHERE deleted_at IS NULL` — chống trùng tên nhưng không chặn tái tạo tên đã xóa mềm; unique thường sẽ chặn nhầm.
- **Pattern nhắc-rồi-xác-nhận:** hệ thống chỉ push nhắc, dữ liệu chỉ sinh khi người xác nhận; mức "tiện" của bước xác nhận tùy bản chất việc (thuốc = ✓ 1 chạm ngay trên noti; gia hạn sub = phải xem xét/trả tiền ngoài rồi mới ghi). Auto-write không confirm để dành AI Bước 2 (có confirm + audit).
- **Bề mặt notification là bề mặt công khai:** text noti đọc được từ lock-screen → cho user tự kiểm soát độ kín đáo (`reminder_text` — "taken micardis?"). Privacy không chỉ nằm trong DB.

## 2026-07-19 — DevOps nền (git/PR/secret)

- **Threat model phải nêu tên cụ thể mới dùng được:** "lo bảo mật" là vô nghĩa; "ngại **social engineering**, không ngại người đọc repo" mới ra được quyết định (giữ public + vẫn cần private mode trong app). Bài học: hỏi *ngại AI nào* trước khi thiết kế biện pháp.
- **Phòng thủ nhiều lớp, chặn càng sớm càng rẻ:** push protection (server, lúc push) + pre-commit gitleaks (local, lúc commit). Lớp local bắt được cả secret tự chế mà pattern provider không biết.
- **Rule phải hợp quy mô đội:** branch protection đòi 1 approval sẽ **tự khóa** dự án một người (không tự duyệt PR của mình được) → đặt `required_approving_review_count: 0` nhưng vẫn bắt buộc PR. Copy best-practice của team lớn mà không chỉnh = tự bắn vào chân.
- **Dựng hàng rào TRƯỚC khi có thứ để rò rỉ:** làm secret-guard lúc repo còn chưa có code/`.env` — rẻ hơn nhiều so với lúc đã lỡ commit.
- **Đọc kỹ ranh giới gói dịch vụ:** "Copilot free có code review" là **sai** — free chỉ review vùng chọn trong IDE, review PR trên github.com cần bản trả phí. Cùng một cái tên tính năng, hai phạm vi khác hẳn (lặp lại đúng bài học "xác nhận đúng category" của phiên hạ tầng).
- **Giao việc cho agent = viết spec, không phải ra lệnh:** spec tự-chứa cần *bối cảnh + lý do*, **phần KHÔNG được làm**, acceptance kiểm chứng được, và đề xuất tier/effort — vì agent chạy ở session 0-context.
- **"Đã cài xong" ≠ "đang bảo vệ":** phải TEST hàng rào bảo mật bằng payload *giống thật*. Key mẫu trong tài liệu AWS không kích hoạt gitleaks (có allowlist cho giá trị ví dụ) → suýt kết luận sai theo cả hai chiều. Test bằng pattern thật mới lộ ra: gitleaks bắt GitHub PAT/Stripe nhưng **bỏ lọt `postgresql://user:pass@host`** — đúng loại secret nguy hiểm nhất của dự án này (Neon). Cùng họ với bài học "backup chưa restore = Schrödinger": công cụ chưa thử = chưa biết nó bảo vệ cái gì.

## 2026-07-20 — Phiên frontend + auth (đóng)

- **"SPA tĩnh" nghĩa đen:** TS/React không bao giờ chạy trên server — Vite build ra `html/js/css`; Node chỉ tồn tại lúc build (Docker multi-stage: stage Node build → copy `dist/` sang image Python); production = đúng 1 process Python serve file + JSON. "Nhiều ngôn ngữ" ≠ "nhiều runtime trên máy chủ" — browser của user mới là runtime thứ hai. Áp: `frontend-brief.md` §4.
- **Cặp quyết định lặp lại được:** React-vs-Svelte lặp đúng cấu trúc FastAPI-vs-Litestar — bên thua "chất lượng thiết kế" thắng ecosystem/AI-assist; khi mục tiêu là dồn năng lượng cho tầng AI, chọn theo "đỡ-bị-kẹt". Nhận ra pattern cũ giúp quyết nhanh mà lý do vẫn khách quan.
- **Sync-engine không phải mặc định của offline-first:** PowerSync/Electric giải bài toán multi-user realtime conflict — không tồn tại ở app 1 người; phần khó của sync (idempotency, undo, retry) đã được giải từ tầng schema (UUIDv7 client + soft-delete) → outbox tự viết là đủ. Bài học: xem bài toán nào *thật sự có mặt* trước khi nhập giải pháp hạng nặng.
- **Supply-chain có hai cửa khác nhau:** code copy-in (shadcn) — rủi ro tại *khoảnh khắc kéo về*, không có kênh silent-update, phòng bằng đọc-diff-lúc-add; npm dependency — rủi ro *mỗi lần install*, phòng bằng lockfile + `npm ci` (làn sóng tấn công npm cuối 2025). Hai loại, hai kỷ luật, đừng lẫn. Áp: `frontend-brief.md` §6.
- **Thiết bị thật của user là ràng buộc thiết kế:** iOS chỉ cho web-push khi PWA đã cài Home Screen (`display: standalone`); action-button trên noti có thể không có → spec "✓ 1 chạm trên noti" phải có fallback 2 chạm; OAuth redirect có thể văng sang Safari (storage tách biệt app-đã-cài) → login loop. Tất cả phải test máy thật, không tin tài liệu chay.
- **Step-up auth (đăng nhập ≠ mở khóa):** một session đăng nhập dài (60–90 ngày, tiện) + một cổng bước-lên ngắn (private 15′, Argon2id) — hai vòng đời cho hai mức nhạy cảm. Điểm khớp threat model "nhìn qua vai": passphrase autofill bằng FaceID → không gõ gì trước mặt người nhìn.
- **AI là một bề mặt hiển thị:** "cho AI toàn quyền khi mở private" nghe như 1 quyết định nhưng chạm 7 bề mặt (read-tools, index bền, LLM provider, transcript, write-tools, cron, client-cache) — mỗi bề mặt một luật (R1–R7, `auth-brief.md` §4). Nhạy nhất là transcript: không gắn cờ private cho message sinh lúc unlocked thì private mode tự thủng qua lịch sử chat. Và giữ rạch ròi: **mã hóa (at-rest) ≠ private (cổng session)** — hai trục trực giao.
- **zdr là hợp đồng, không phải toán học:** zero-data-retention của LLM provider là cam kết chính sách — muốn AI đọc sâu dữ liệu riêng qua bên thứ ba thì mức bảo đảm tối đa là "niềm tin có hợp đồng + chọn provider nghiêm". Ghi thẳng vào decision record để khẩu vị rủi ro là lựa chọn có ý thức, không phải ngầm định.

## 2026-07-20 — Phiên harness công cụ dev (đóng, cùng ngày)

- **Thị trường coding-plan đổi theo quý — giá promo là mồi:** GLM \$3/mo (2025) chết sau ~5 tháng, giá chuẩn ×3–6; Qwen đóng free tier; MiniMax entry ×2. Quyết định công cụ = decision record có ngày + re-check date, y hệt quyết định hạ tầng (lặp bài "đọc giá gia hạn, không tin giá năm-đầu" — lần này áp cho chính bộ đồ nghề).
- **Phân tầng model theo blast-radius của lỗi, không theo độ to của việc:** nguyên tắc sequencing AI của chính app (Bước 0→3) áp ngược lên dev-tooling — auth/DDL ("sai âm thầm thành nợ") đi model mạnh nhất; CRUD/bulk đi workhorse; vòng lặp đi bậc rẻ. Một nguyên tắc, hai chỗ dùng.
- **Chiến lược quota = protocol, không phải mua thêm:** thứ đốt limit không phải task khó mà là vòng lặp thử-sai + test-spam → flow đẩy loop xuống tầng quota-rộng (Luna/T3); luật một dòng "Sol chỉ nhận việc đã có spec".
- **"Model phế" là phế-tương-đối-với-việc:** Gemini không đủ tin làm agent thi công (kinh nghiệm thật của chủ), nhưng "chạy test theo hướng dẫn" là việc khối-lượng-lớn/phán-đoán-thấp — khớp đúng hồ sơ quota-nhiều-chất-vừa. Xếp việc theo chất model, thay vì loại model theo cảm giác chung.
- **Chi tiết vận hành địa lý lật được bảng so giá:** quota multiplier giờ-cao-điểm của gói Trung trùng giờ làm việc VN → giá danh nghĩa rẻ nhưng quota hiệu dụng co đúng lúc mình cần. So giá phải so ở "chi phí/đơn-vị-việc *lúc mình làm việc*".
- **Benchmark đo "tay", không đo "óc giữ luật":** Terminal-Bench chọn được executor (Codex); vai "giữ ngữ cảnh + ritual + review" phụ thuộc nơi context sống (hệ Claude với briefs/memory) — thắng bench không tự động thay vai T1. Cùng họ: ranh giới code-public/data-private quyết *routing tool bên thứ ba*, mở rộng bài "threat model phải nêu tên cụ thể".

## 2026-07-21 — Phase B thi công: chuỗi 003→006 chạy thật (walking skeleton có DB)

- **Ràng buộc mã hoá hỏng *im lặng*, không hỏng ồn ào:** unique index trên cột mã hoá AES-GCM **không bao giờ bắt trùng** (nonce ngẫu nhiên → cùng chữ ra khác bản mã), nhưng vẫn nằm đó trông như đang bảo vệ; nếu đổi sang mã hoá tất định cho index chạy đúng thì *chính index* thành chỗ rò (so bằng + đếm tần suất). Đây là loại lỗi tệ nhất: test xanh, CI xanh, sai lộ ra sau một năm dữ liệu. Áp: K19, và là lý do §7.1 chọn **cấm thẳng** thay vì cân đo %.
- **Quyết định mới có thể làm sai quyết định cũ đã ✅ CHỐT:** phiên encryption đóng *sau* K1–K17 nên K2 (unique index tên) và K5 (CHECK ≥0 tiền) thành sai **mà vẫn mang dấu ✅**. Executor đọc K2 rồi làm đúng-như-dặn sẽ dựng thẳng cái bẫy trên. → Chuỗi phiên độc lập **bắt buộc** có một lượt quét chéo trước khi đúc (K18–K21 sinh ra vì thế). Bài học tổng quát: decision record không tự vô hiệu hoá nhau, phải có người đi vá.
- **HITL trả lãi ngay lần đầu dùng:** giao thức "gặp fork thật thì DỪNG + trình bày 5 mục" khiến executor dừng **trước khi viết DDL** và lôi ra 4 lỗ hổng thật (K22–K25). Nếu để nó tự lấp, cả 4 chỉ lộ ra sau khi có data — đúng lúc sửa đắt nhất. Kèm định nghĩa *fork thật vs không thật* để nó không hỏi tên biến.
- **Uỷ quyền tốt = "duyệt sau theo danh sách", không phải "quyết trước từng cái":** chốt tay hàng trăm cột nullability là đốt một buổi cho thứ không có ý nghĩa sản phẩm; K25 đổi thành *executor tự quyết nhưng phải liệt kê thành bảng trong PR*. Lượt duyệt đó bắt được K26 (bắt buộc tiêu đề note = phá capture-một-chạm) — đúng loại lỗi chỉ người-hiểu-sản-phẩm mới thấy.
- **"Build local xanh" không chứng minh gì về thứ đã commit:** `.gitignore` khuôn Python (`lib/`) nuốt im lặng `frontend/src/lib/utils.ts` của shadcn → máy dev xanh vì file có trên đĩa, CI đỏ vì file không có trong git. Chính là việc CI tồn tại để làm. Kéo theo luật `AGENTS.md`: sau `git add` phải xác nhận file vào index.
- **Exit code chỉ nói "tôi bị giết", không nói "chưa có gì xảy ra":** `npm install` timeout ở 124s nhưng đã ghi xong lockfile 263KB + `node_modules` đầy đủ; executor tin exit code, báo "chưa cài được gì" và dừng nhầm. Với lệnh dài (npm/docker/uv) phải **kiểm trạng thái thật trên đĩa** trước khi kết luận.
- **PowerShell nuốt dấu `"` trong tham số native — một bug, hai hình dạng:** phá mô tả PR (mất sạch dấu tiếng Việt → `?`, **mất hẳn không decode ngược được**) *và* phá lệnh `gh --jq` (jq đọc `"protect-main"` thành phép trừ). Luật: text tiếng Việt luôn qua file UTF-8 (`--body-file`/`commit -F`); lọc JSON dùng `ConvertFrom-Json | Where-Object`.
- **PostgreSQL 16+ đổi luật tạo role:** role **không phải superuser** tạo role mới sẽ được cấp `ADMIN TRUE` nhưng **`INHERIT FALSE, SET FALSE`** — cố ý, để "tạo role" không âm thầm thành "có quyền role đó". Neon `neondb_owner` chính là non-superuser nên `SET ROLE` thất bại. Gỡ bằng `GRANT … WITH SET TRUE`, và **lệnh đó phải nằm trong script bootstrap được commit** — tri thức chỉ-có-bằng-cách-va-phải thì phải sống trong repo, không trong đầu ai.
- **Bar bảo mật phải tương xứng với tài sản, không với nỗi sợ:** chủ phản biện luật "cột mã hoá không bao giờ có embedding" — mối đe doạ nó chặn chỉ khai thác được bởi kẻ *đã có credential DB*, nằm ngoài threat model đã ghi, còn giá phải trả là mất tìm-kiếm-ngữ-nghĩa vĩnh viễn cho một app AI-first. Nhận diện đúng: **last-mile theo nghĩa xấu** — tốn rất nhiều công cho mức bảo vệ không ai cần. Nhưng tách bạch: giữ *mã hoá-lúc-lưu* (rẻ, chặn kịch bản thật), chỉ đem *luật cấm embedding* ra cân. Và **hoãn quyết** vì cột `vector` chưa dimension/chưa index → chưa có gì đang bị chặn, quyết bây giờ là quyết mù.
- **Secret không lên môi trường chạy chỉ vì "nó là DB URL":** ba role ba vai (`neondb_owner` bootstrap-only ở máy chủ · `microsched_migrator` sở hữu schema + chạy Alembic, sau này CI · `microsched_app` chỉ CRUD, **biến duy nhất lên Fly**). Nhầm hai biến vẫn *chạy được* nên không ai phát hiện — đó chính là lý do phải viết thành bảng có vai rõ ràng. Kiểm chứng thật: app role bị chặn `CREATE TABLE`/`DROP TABLE`, vẫn `INSERT/SELECT` bình thường.
- **Đặt secret ≠ deploy:** `flyctl secrets set` chỉ **restart** máy, không build lại image → sau merge 006, site vẫn chạy image từ 005 và `healthz` thiếu trường `db`. Không có CI-deploy thì **mỗi task merge xong là repo tiến, site đứng yên** — lệch trạng thái ở tầng deploy, cùng họ với split-brain dữ liệu mà dự án này sinh ra để tránh.

## 2026-07-21 — 007 auth: walking skeleton khép, và bài học lớn nhất không nằm ở auth

- **Vòng lặp kiểm chứng thắng sức mạnh model — đo được trong cùng một model, cùng một ngày.** 007 do T1 (Opus 4.8) thi công: tầng có pytest (logic auth, 34 case) **0 lỗi lọt**, kể cả security-review Opus MAX chạy riêng cũng không tìm ra HIGH/MEDIUM; tầng **không có vòng lặp nào** (frontend runtime, trình duyệt, bản deploy) **4 lỗi lọt tới chủ**. Biên độ chênh đó lớn hơn khoảng cách giữa các tier model. ⇒ **Sức mạnh model và vòng lặp là hai trục khác nhau, trục vòng lặp thắng**; model yếu mà *chạy thử rồi mới nói* đáng tin hơn model mạnh *suy luận rồi khẳng định*. Áp: quy ước báo cáo tách **đã-chạy / chỉ-suy-luận** (`agent-tasks/README.md`), và phân việc thêm trục "ai lái được trình duyệt" chứ không chỉ blast-radius (`devops-brief.md` §7.1).
- **"Build xanh" là bằng chứng về *biên dịch*, không phải về *hành vi*.** Sai lầm nặng hơn bản thân bug: báo cáo kết quả suy luận bằng đúng giọng của kết quả đã kiểm chứng. Lòng tin hỏng ở chỗ đó, không ở chỗ có bug.
- **Nhóm `dev` che được dependency production bị thiếu.** `httpx` ở nhóm dev (cho TestClient) → pytest xanh, image `--no-dev` không import nổi app. Viết thêm bao nhiêu test cũng không lộ ra — **giới hạn cấu trúc, không phải thiếu sót**. Hàng rào đúng là job CI cài `--no-dev` rồi thử khởi tạo app. Cùng họ với bài `.gitignore` nuốt `lib/utils.ts` (003): *môi trường dev khác môi trường thật ở chỗ nào thì lỗi nấp ở đúng chỗ đó*.
- **Nhánh lỗi và nhánh thành công có bề mặt rò rỉ khác nhau.** `?code=` chỉ nằm lại trên URL ở **nhánh bị từ chối** (nhánh hợp lệ redirect đi nên sạch). Chỉ test happy path là mù đúng nửa còn lại. Chủ tìm ra bằng **đối chiếu URL hai nhánh** — kiểm thử khác biệt, không phải theo checklist.
- **PWA nuốt route server-side:** `vite-plugin-pwa` mặc định cho SW trả `index.html` cho *mọi* điều hướng → `/auth/login` không bao giờ tới backend, nút bấm im lặng không làm gì. Bất kỳ route nào do server xử lý phải vào `navigateFallbackDenylist`. Lỗi này **chỉ tồn tại ở bản build production** — dev server không đăng ký SW.
- **SSO của nhà cung cấp làm nút đăng xuất của mình thành vô nghĩa:** xoá phiên phía mình xong, Google vẫn giữ phiên của nó nên đăng nhập lại diễn ra im lặng, không qua bước nào. `prompt=select_account` khôi phục đúng kỳ vọng "đăng xuất rồi thì phải đi qua Google lại".
- **Sửa bằng cách BỎ chỗ mình đoán sai, thay vì đoán lại cho đúng hơn:** hai lần liên tiếp sai về ngữ nghĩa cache TanStack Query (`invalidate` → `remove`) đều do suy luận không chạy thử. Lần ba đổi hướng: đăng xuất bằng **điều hướng thật**, để tính đúng đắn không còn phụ thuộc vào hiểu biết đã hỏng hai lần. *Khi đã sai hai lần về một cơ chế, đừng sửa cho khéo hơn — hãy gỡ sự phụ thuộc vào nó.*
- **Nhiều session cùng working tree làm bẩn commit — va ngay hôm viết luật:** một session khác escape `$`→`\$` trong 8 file docs (bẫy inline-math của Markdown Preview Enhanced) trong lúc T1 đang làm; `git add -A` gom hết vào commit fix logout, phá quy ước *1 commit = 1 phiên quyết định*. Đúng rủi ro đã ghi ở `devops-brief.md` §8 cùng ngày. ⇒ Luật thực dụng: nhiều session song song thì **`git add` theo đường dẫn cụ thể, không bao giờ `-A`**.

## 2026-07-22 — sau skeleton: một sự cố tiền, và ba bài về chỗ lỗi ẩn náu

- **Lỗi nguy nhất nằm GIỮA hai quyết định đều đúng, ở hai file không tham chiếu nhau.** `schema-physical-brief.md` §185 (19/07) viết *"cron đừng ping DB quá dày"*; `fly.toml` của 005 (20/07) đặt health check 30 giây trỏ vào endpoint có `SELECT 1`; Neon ngủ sau 5 phút ⇒ **DB không bao giờ ngủ** ⇒ 6 CU-hrs/ngày trên hạn mức 100 ⇒ compute treo ~06/08 = **mất DB 13 ngày**. Cả hai quyết định đều đúng; lỗi sinh ra ở khoảng trống giữa chúng. **Không vòng lặp nào hiện có nhìn thấy nó**: không exception, không log, không test đỏ, không endpoint chậm, security-review cũng không (đây không phải lỗ bảo mật). ⇒ Thấy một **ràng buộc vận hành** trong brief thì phải đi grep các file cấu hình có thể phạm — và **để chú thích cấm ngay tại chỗ dễ tái phạm** (`fly.toml`), vì người tái phạm sẽ đọc file đó chứ không đọc brief.
- **Test cho hành vi VẮNG MẶT là thứ không ai nghĩ ra khi viết test cho tính năng.** Hàng rào duy nhất chặn tái phạm là `assert_not_awaited()` trên đường liveness. Lỗi vô hình (không hỏng gì, hoá đơn tới sau hai tuần dưới dạng sự cố) thì **không gác được bằng mắt người review** — phải gác bằng test, và phải cố ý nghĩ ra nó.
- **Một quy tắc không có điều kiện kích hoạt thì không bao giờ kích hoạt.** `devops-brief` §2 ghi *"`main` = trạng thái đã duyệt"* — nghe hợp lý, và `main` **chết trong im lặng**: tụt 33 commit, đứng ở PR #4, cũ hơn cả lần deploy thành công đầu tiên. Không phải lười, mà là chưa ai định nghĩa *điều kiện*, nên không bao giờ tới lúc "đủ điều kiện". Sửa bằng định nghĩa kiểm chứng được: **`main` = bản đã deploy VÀ đã được kiểm chứng** + tag `v0.x`. ⇒ Đặt tên trạng thái ("đã duyệt", "ổn định") không thay được việc nói rõ *cái gì xảy ra thì mới chuyển*.
  - **📝 2026-07-22 (muộn) — chính bài học này suýt tái phát ở tầng khác.** Bản vá trên chốt *"từ 008b, merge vào `main` là trigger deploy"*, mà điều kiện merge lại là *"đã nghiệm thu trên fly.dev"* — **vòng tròn**: thứ duy nhất đưa code lên fly.dev chính là merge vào `main`. Hai câu, cùng một file, cùng một ngày. → Đảo lại: trigger deploy về `develop`, `main` thành nhãn release (`devops-brief.md` §2.1). **Bài học chồng lên bài học:** một điều kiện kích hoạt kiểm chứng được vẫn có thể **không bao giờ thoả** nếu nó phụ thuộc vòng vào chính hành động nó đang canh. Viết xong điều kiện thì hỏi thêm một câu: *"cái gì làm cho điều kiện này thành đúng, và nó có nằm sau chính cánh cửa này không?"*
- **Số "trông ổn" vẫn có thể là phát hiện — bất thường là độ lệch khỏi MÔ HÌNH, không phải độ lớn.** Con số bắt được sự cố là **4,54/100 CU-hrs**, tức 4,5% hạn mức: mọi ngưỡng đều xanh. Thứ bắt được nó là chủ nhìn **hình dạng biểu đồ** và hỏi *"sao chạy liên tục? web đâu có request?"*. ⇒ Câu hỏi lõi khi soi hoá đơn là **"mình giải thích được số này không?"**, không phải "số này có ổn không"; **không giải thích được thì điều tra, bất kể số nhỏ**. Hệ quả cho tự động hoá: script in `4.54 ✅ dưới ngưỡng` sẽ **bỏ sót đúng ca này** ⇒ chỉ tự động hoá phần có ngưỡng rõ, **giữ nguyên bước người nhìn biểu đồ**.
- **Canh dòng có trần, chặn dòng không trần.** Mảng theo dõi tiền lúc mới mở canh Fly + Neon — mà cả hai **tự có trần** (1 máy cố định / free tier hết thì treo). Dòng thật sự nguy là **LLM API Bước 1**: usage-based, không trần, một vòng lặp agent lỗi đốt sạch trong một đêm. Dashboard là *quan sát*, không phải *phòng thủ*; dòng không trần phải chặn bằng **hard spending limit ở nhà cung cấp**.
- **Hoá đơn mang dấu vết của bug.** Fly tính theo giây (kiểm chứng: 1 unit = 1 giây, khớp thời điểm sinh machine trong 26 giây) — nên ngày 21/07 hụt ~72 phút so với ngày đầy chính là các lần restart + crash-loop `httpx` của 007. Và "xu hướng tăng" của Fly hoá ra chỉ là **ảo giác của ngày đầu cụt** (máy sinh lúc 21:00). ⇒ Trước khi kết luận về một xu hướng, kiểm xem điểm dữ liệu đầu tiên có phải một kỳ đầy đủ không.

## 2026-07-22 (muộn) — 008b: CD chạy thật, và hai lỗi "đúng chức năng, sai chỗ đặt"

- **Deploy tự động không phải tiện nghi mà là hạ tầng của kỷ luật kiểm chứng.** Từ 008 mỗi lát cắt đều phải nhìn bằng mắt trên bản deploy thật; deploy thủ công là ma sát đặt đúng ở bước đã để lọt 4 lỗi của 007. Đặt CD **trước** task đặt khuôn để cái khuôn được đúc lúc đã có sẵn quy trình nghiệm thu.
- **Smoke test kiểm `status` là chưa đủ — phải kiểm ĐÚNG BẢN NÀO đang chạy.** Nếu deploy hỏng một phần và Fly giữ máy cũ đang chạy tốt, smoke test kiểu cũ **xanh trên bản cũ** = báo thành công cho một lần deploy thất bại. Vá bằng cách bơm git SHA qua build-arg vào `/api/readyz` rồi khẳng định SHA khớp commit vừa merge. Nghiệm thu thật: `readyz.commit` = đúng merge commit.
- **Hai lỗi review đều thuộc một lớp: code đúng chức năng, sai VỊ TRÍ — và CI không bao giờ thấy lớp này.**
  - `ENV GIT_SHA` đặt ở tầng 2 của Dockerfile: Docker build lại mọi tầng **dưới** tầng đầu tiên bị đổi, mà mã commit đổi mỗi deploy ⇒ cài lại toàn bộ dependency mỗi lần. Dời xuống cuối: **32,8s → 2,5s** (đo thật, mọi tầng `CACHED`). ⇒ vào hàng đợi học, mục D1 dưới.
  - `git describe --tags` hỏi từ `develop`: **không bao giờ** tìm thấy tag, vì Git chỉ đi ngược lên tổ tiên còn mọi tag nằm trên merge commit của `main` — tức *con cháu* của `develop`. Ô "tag gần nhất" sẽ in "chưa có" vĩnh viễn. **Một trường luôn sai trong bảng cảnh báo còn tệ hơn không có bảng**: nó dạy người ta ngừng đọc, và phanh hỏng trong im lặng.
- **Đừng suy ra môi trường từ một setting hàng xóm.** Guard mới ban đầu đoán "có phải production không" bằng cách đọc `SESSION_COOKIE_SECURE`. Chạy đúng, nhưng dựng một sợi dây vô hình: một ngày tắt cookie-secure vì lý do riêng của nó sẽ tắt luôn guard chẳng liên quan. → Sinh `APP_ENV`, hỏi qua `settings.is_production`. Hai tính chất cố ý: **mặc định là `production`** (mất một dòng thì rơi về bản nghiêm, không bao giờ nới ra) và **kiểu `Literal` chứ không `str`** (`APP_ENV=prod` là lỗi gõ mà nếu so chuỗi sẽ *âm thầm* thành "không phải production" — tái tạo y nguyên loại lỗi đang đi sửa).
- **Trùng lặp có chủ đích không phải thừa.** `fly.toml [env]` ghi `APP_ENV = 'production'` *đồng thời* code mặc định `production`: đọc cấu hình là biết ngay production đang ở chế độ nào, mà mất dòng đó vẫn an toàn. Bản đầu bỏ trống trên Fly để lấy tính an toàn — nhưng tính an toàn đó đã do mặc định cấp rồi, nên bỏ trống **không mua thêm gì mà mất khả năng đọc-là-biết**.
- **Một cơ chế CÓ trong code vẫn có thể CHƯA hoạt động** vì luật của hệ thống khác: `workflow_dispatch` chỉ mở khi workflow đã có trên **default branch**, nên cả đường lùi phụ lẫn nút chạy cron tay đều chưa tồn tại cho tới lần merge vào `main` kế tiếp. ⇒ Với mọi "đường thoát hiểm" dựng thêm, **hỏi ngay nó đã dùng được chưa** — đừng chờ tới lúc cần.
- **Test phải được chứng minh là biết đỏ.** Áp hai lần trong phiên: đổi mặc định sang `local` + nới `Literal` cho nhận `"prod"` ⇒ đúng 2 test failed, hoàn nguyên ⇒ 43 passed. Một test chưa từng đỏ là một test chưa từng được kiểm.

## 2026-07-22 (cuối ngày) — phiên harness: Claude điều phối Codex

- **Phân rã rủi ro trước khi chấp nhận nó.** Chính chủ nói *"chấp nhận thêm 40% risk"* như một khối. Tách ra thì đó là ba thứ — ⓐ không đọc spec trước, ⓑ **không đọc báo cáo executor trước khi Claude tin nó**, ⓒ mất nhịp nghỉ — và **chỉ ⓑ đáng sợ**, vì đã có số đo (task 004: executor khai sai về chính việc nó vừa làm). Bài học chung: một con số rủi ro tổng không hành động được; **rủi ro chỉ vá được sau khi biết nó vào bằng cửa nào**. Vá đúng ⓑ = luật biên lai, hai cửa còn lại để nguyên.
- **Thiết kế phép đo ra HIỆU SỐ, đừng ra con số tuyệt đối.** Câu hỏi *"Codex chạy qua plugin có được nạp memory không"* không có trong tài liệu và **không suy ra được từ kết quả công việc**. Chạy một lần chỉ cho một bức ảnh — mà không có mốc so thì bức ảnh đó vô nghĩa. Chạy **cùng một prompt tự-khai trên hai bề mặt (app / plugin) rồi lấy hiệu** thì chính chênh lệch *là* câu trả lời, kể cả khi cả hai bản đều có chỗ model tự bịa (bịa giống nhau ⇒ triệt tiêu khi trừ). Áp: `agent-tasks/harness-audit/01`.
- **Hỏi thẳng cấu hình, đừng suy từ hành vi.** Bản đầu của bài kiểm định "có memory không" là *quan sát xem nó có nhớ việc cũ không* — chính chủ bác: hỏi thẳng nó thấy gì thì rẻ hơn và ít nhiễu hơn. Kèm một chi tiết quyết định độ tin: bắt **tách "khối memories trong context" khỏi "thư mục memories trên đĩa"** và **cấm** dùng cái sau để trả lời cái trước — không có ranh giới đó, model `ls` thấy thư mục rồi kết luận "có, tôi nhớ".
- **Khi một kết luận dựa trên một ràng buộc, nước đi đáng giá là tấn công ràng buộc — không phải cãi lại kết luận.** `devops-brief` §8 kết luận *song song vô ích vì nút cổ chai là băng thông review của chủ*. Không bác được kết luận đó. Nhưng **thang triage L1/L2/L3** làm giảm chính lượng-đọc-mỗi-luồng ⇒ phép tính của §8 đổi ⇒ mở lại được. Cùng lúc rút ra: thang có tác dụng **ngay ở một luồng**, nên phần lớn "hiệu suất cộng thêm" không nằm ở số luồng.
- **Uỷ quyền quyết định thì phải nói rõ quyết định ĐI ĐÂU, không chỉ nói ai quyết.** Thang chỉ an toàn nhờ luật ④: **mọi L2 phải hiện trong PR description**. Không có nó, "Claude tự quyết" = quyết định biến mất; có nó, **không quyết định nào biến mất — chỉ đổi chỗ chủ đọc**, từ chat sang mô tả PR, đúng lúc chủ cầm nút merge. Quyền phủ quyết còn nguyên, chỉ dồn về một điểm.
- **Lời giải của ràng buộc A có thể làm TỆ HƠN ràng buộc B** — biến thể thứ tư của lớp lỗi "khoảng trống giữa hai quyết định đều đúng". §8 kê *"mỗi agent một Neon branch"* (chữa va chạm schema, đúng); `cost-brief` §7 ghi *"Neon mong manh"* (đúng). Tra live mới thấy: **100 CU-hours là hạn mức của cả project, mọi branch xài chung một túi**, cộng autosuspend 5 phút ⇒ mỗi lần đánh thức tính tối thiểu ~5 phút compute dù test chạy 10 giây. Ba luồng chạy *test-fix-test* đốt hết trong một buổi chiều. Bài học: khi áp một lời giải, **đi soi ràng buộc của các file KHÁC mà lời giải đó chạm vào** — không chỉ soi xem nó có chữa đúng bệnh không.
- **Bộ nhớ agent có hai hạng lưu trữ, đừng nhầm:** *instruction file* (`AGENTS.md` — bền, người viết, review được, đi theo git) và *generated state* (`~/.codex/memories/` — máy sinh, có lifecycle consolidate **sẽ nuốt mất thứ đặt tay vào**). ⇒ Thứ **phải luôn áp dụng** thì viết vào hạng một; hạng hai chỉ là lớp gợi nhớ. Đây cũng là lý do **không** copy bộ nhớ giữa hai harness: nhân bản một sự thật sang store thứ ba là split-brain ở tầng chỉ dẫn — cùng anti-pattern mà `CLAUDE.md` mở đầu bằng nó ở tầng dữ liệu.
- **Thêm một tầng lọc chỉ tiết kiệm khi nó TỪ CHỐI được rẻ.** Lọc PR của executor do mình brief thì rẻ (có sẵn mô hình "đáng lẽ thế nào"); lọc PR của agent chạy tự do thì đắt — phải dựng lại ý định từ diff, gần bằng tự viết. ⇒ Quyết định mở lane Jules bằng **hai số đo** (tỉ lệ PR được nhận · thời gian lọc mỗi PR), không bằng ấn tượng về chất lượng model.
- **Khi dữ liệu không phải secret, `.gitignore` là cơ chế bảo vệ duy nhất — và phải kiểm bằng lệnh.** Báo cáo về thói quen làm việc không kích hoạt gitleaks (nó tìm token, không tìm chân dung). Repo public ⇒ chọn *giữ README trong git, chặn nội dung*. Và kiểm bằng `git add -n` chứ không nhìn mắt: `git check-ignore` với pattern phủ định đọc dễ nhầm, còn dry-run in ra **đúng thứ vào được index**.

## 2026-07-23 — audit harness 01+02: đo một cỗ máy bằng chính nó

- **Phép đo bằng hiệu số (differential measurement):** chạy **cùng một prompt** trên hai bề mặt rồi **lấy hiệu**, thay vì hỏi một bề mặt "anh thấy gì". Lý do sâu: phần model **bịa giống nhau ở cả hai bản sẽ triệt tiêu khi trừ**, chỉ chênh lệch thật còn lại. Một lần chạy cho một bức ảnh; hai lần cho một **phép trừ**. Áp: `harness-audit/01`, và nó trả lời được thứ tài liệu OpenAI không nói (phiên app-server có memory không).
- **⚠️ Phép đo bị nhiễu (confound) — bài đắt nhất của phiên:** hai trường "ghi file được không" **trông như** phát hiện, thực ra là **tiếng vọng của chính cái prompt**. Prompt tự khai *"đây là phiên chỉ-đọc"* → subagent đúng luật khi bỏ cờ `--write` → sandbox thành `read-only` → báo cáo "không ghi được". Tức **công cụ đo bị chính thứ đang đo cấu hình lại**. Bài học tổng quát: khi thiết kế một phép thử, hỏi *"kết quả này có thể do chính cách tôi hỏi tạo ra không?"* — và spec **tự mâu thuẫn** (vừa cấm sửa file, vừa bắt lưu output ra file) là dấu hiệu sớm nhất.
- **Neo đường dẫn trong `.gitignore`:** mẫu có `/` **ở giữa** thì git **neo vào thư mục chứa `.gitignore`** (gốc repo); `harness-reports/*` không khớp `agent-tasks/harness-reports/`. Tiền tố `**/` mới khớp mọi độ sâu. Và phải giữ `/*` (nội dung) chứ không dùng `/` (thư mục), vì **git cấm re-include một file khi thư mục cha đã bị loại** → dòng `!…README.md` sẽ chết theo. Kiểm bằng `git check-ignore -v`, **không** bằng đọc rule.
- **Hai loại câu trong một tài liệu cần hai luật bảo trì NGƯỢC nhau:** *current state* phải **sửa đè** (giá trị nằm ở chỗ nó đúng lúc này); *quyết định* phải **cộng thêm dated note, không viết đè** (giá trị nằm ở chỗ nó ghi được vì sao). `CLAUDE.md` trộn hai loại vào **cùng một đoạn văn** → đầu đoạn nói "pre-code", cuối đoạn nói "003–007 DONE", sống 2 ngày. Đây là cách một file luật tự nói dối.
- **Generated memory là *biên lai của một thời điểm*, không phải *trạng thái hiện tại*:** hai trong ba "mâu thuẫn" mà cross-audit nêu (PR #13, task 01 thiếu bản B) hoá ra **không phải mâu thuẫn** — chỉ là memory Codex đóng băng lúc phiên kết thúc, không có đường học chuyện xảy ra sau. ⇒ Khi đọc `rollout_summaries/`: lấy *cách gỡ*, vứt *trạng thái*.
- **Chỉ thị tới nơi ≠ chỉ thị được thi hành:** Codex **đọc được** dòng bắt buộc in ra (trích lại nguyên văn trong báo cáo) nhưng hiểu thành lời khai của caller nên không in. Không lỗi, không cảnh báo. ⇒ Nghiệm thu phải kiểm **dòng chữ thật trong sản phẩm**, không suy từ "prompt đã gửi đúng" — luật biên lai mở rộng xuống tầng định dạng.
- **Metadata commit là một bề mặt rò riêng:** merge bằng nút web GitHub ghi **email tài khoản** vào author của merge commit, độc lập hoàn toàn với `git config user.email` local. Nội dung file thì grep ra được và sửa được; metadata thì nằm trong lịch sử. *(Xử lý: phiên bảo mật song song.)*

## 2026-07-23 (phiên bảo mật) — soi toàn dự án thay vì soi diff

- **Phạm vi của một công cụ kiểm tra suy ra từ quy ước git thì chết cùng lúc quy ước đó đổi — và chết im lặng.** `/security-review` lấy scope `git diff main...develop`. Quy ước đó đúng khi `main` nghĩa là "phần đã duyệt"; từ 22/07 `main` chỉ còn là nhãn release đang tụt (§2.1) ⇒ cùng một lệnh soi **11/47 file code (mù 77%)**, và vùng mù chứa đúng `deps.py`/`sessions.py`/`domain/auth.py` — tầng bảo mật lõi. Không có cảnh báo nào; báo cáo vẫn trông đầy đủ. Ngược đời hơn: nếu `main` **bằng** `develop` thì scope thành rỗng và công cụ báo "không có gì để soi". ⇒ **Trước khi tin một báo cáo kiểm tra, hỏi nó đã ĐỌC gì — đừng hỏi nó KẾT LUẬN gì.**
- **Kết luận rút từ diff có thể đúng mà vô căn.** Bản review đầu kết luận *"cron auth an toàn: `compare_digest` constant-time, token rỗng thì 503"* — trong khi **không đọc `deps.py`**, file chứa chính hàm đó (nó nằm ngoài diff, và báo cáo tự ghi *"out of scope"*). Kết luận tình cờ đúng. ⇒ Ranh giới **đã-đọc / chưa-đọc** phải hiện trong báo cáo y như ranh giới **đã-chạy / chỉ-suy-luận** đã chốt sau 007 — cùng một luật, khác tầng.
- **Threat model quyết định LOẠI review, không phải ngược lại.** `devops-brief` §1 ghi threat model là **social engineering**, không phải người đọc code. Vậy mà toàn bộ review đầu soi code — tức soi đúng cái threat model *không* nêu. Soi `docs/` mới chạm đúng bề mặt, và ở đó có thật: hồ sơ sức khoẻ ghép được từ 3 file + git metadata. ⇒ Trước khi soi, đọc threat model rồi hỏi *"bề mặt này có phải bề mặt nó nói không?"*
- **Luật phân loại theo LOẠI FILE thì nội dung nhạy viết trong file đó thừa hưởng nhãn mà không ai phải quyết định.** `devops-brief` §7 luật 1: *"code + docs = public"*; luật 3: *"data thật → chỉ tool local"*. Luật 3 canh data chảy **ra** tới agent; **không luật nào canh data chảy VÀO repo**. Sự thật đời tư viết bên trong một file docs rơi đúng khoảng hở — được dán nhãn public một lần, xong. ⇒ Luật ranh giới phải phân loại theo **nội dung**, không theo **vật chứa**.
- **Cùng một bất biến áp khác nhau ở hai bảng = câu hỏi chưa từng được quyết, không phải ai đó quên.** `task` bắt mã hoá **cả title** khi private; `note` chỉ bắt **body**. Soi ra: `tracking-brief` §6 chốt chữ *"private note/task **bodies**"* ⇒ `note` đúng spec, `task` **chặt hơn** spec. Không bên nào sai — câu hỏi *"tiêu đề note private có phải secret không"* bị quyết ngầm hai lần theo hai hướng, ở hai chỗ không tham chiếu nhau. ⇒ Thấy hai chỗ áp cùng luật ở hai mức, **đừng vá cho bằng nhau — đi tìm quyết định gốc, nhiều khả năng nó chưa tồn tại.** (→ `agent-tasks/008d`, phải quyết trước 008 vì bảng còn rỗng: sửa bây giờ là DDL thuần, sửa sau là migration có backfill.)
- **Biên bản mô tả nhỏ hơn hiện thực thì thôi làm biên bản.** §1 ghi khoản đánh đổi public là *"§12 có một ví dụ nêu tên một loại thuốc thật → đã cân nhắc và chấp nhận"*. Hiện thực: tên thuốc ở **ba** file + giờ sinh hoạt + hồ sơ hành vi + danh tính trong git. Tác hại không phải hôm nay mà **lần đọc sau**: thấy "đã cân nhắc rồi" nên không soi lại. Cùng cơ chế đã giết định nghĩa `main`. ⇒ Dòng ghi nhận một đánh đổi phải **liệt kê đủ**, không tóm tắt — vì công dụng duy nhất của nó là được đọc lại bởi người đã quên.
- **Một lệnh cấm phát biểu theo MỘT chiều thì chiều kia mặc nhiên được phép — và không ai đọc ra vế đó.** Luật đã chốt: *"cột mã hoá không bao giờ vào FTS/embedding"*. Vế chưa ai viết: **cột trần thì ĐƯỢC PHÉP vào**. `note.title` để trần khi private ⇒ đủ điều kiện đi vào pgvector ⇒ tiêu đề note riêng tư hiện ra lúc hỏi AI, **kể cả khi private mode đang khoá** — một cửa rò không liên quan gì tới "ai đó lấy được dump", và chính nó mới là lý do quyết. ⇒ **Viết luật cấm thì viết cả hai vế**, hoặc ít nhất tự hỏi *"cái không bị cấm thì có đang được cho phép không?"*
- **Dấu `*` trong một ô bảng quyết định là cách viết cấm dùng.** `task.*` và `note.body_md` nằm cùng một ô §6, viết một lượt, ở hai mức chi tiết. Hai DDL thi hành trung thực ⇒ hai kết quả khác nhau ⇒ **không ai sai spec, mà vẫn lệch**. Không có công cụ nào bắt được: `*` là chữ hợp lệ trong văn bản, còn CI thì chỉ thấy hai constraint đều chạy tốt. ⇒ Trong decision record, **liệt kê đủ tên cột, kể cả khi dài**.
- **Ranh giới của một cuộc review là ranh giới của rubric, không phải của rủi ro — nên phải nói ra chỗ mình bị cấm nhìn.** Rubric loại "resource exhaustion", mà rủi ro vận hành thật nhất lại đúng hình dạng đó (`/api/readyz` ẩn danh chạy SQL = tái tạo sự cố Neon 21/07 một cách cố ý, hậu quả kéo dài nhiều tuần sau khi ngừng). Báo cáo tách riêng mục *"ngoài rubric nhưng quan trọng"* thay vì im. ⇒ **Im lặng vì rubric là một dạng nói dối bằng cách bỏ sót.**

## 2026-07-23 (phiên hạ tầng runtime) — chủ tự đo rồi bắt lại quyết định của tôi

- **Một ô "đổi lại" gộp nhiều phương án thì con số của phương án tệ nhất lây sang cả rổ.** `architecture-brief` §64 viết *"loại thẳng mọi phương án scale-to-zero (Render free, Fly-autostop)"* — một câu, hai dịch vụ, **một con số** (30–60s, của Render). Fly-autostop bị loại bằng số của hàng xóm, **chưa ai đo**. Ràng buộc gốc ("cold-start là dealbreaker") **đúng hoàn toàn** và vẫn đúng — nó chỉ bị áp lên một con số không tồn tại. Số thật: **~8s, kể cả sau 10h38 ngủ**. ⇒ Trong bảng so sánh, **mỗi phương án bị loại phải có số riêng của nó**, kể cả khi kết luận giống nhau — vì kết luận thì tái sử dụng được, còn con số thì không.
- **Thứ đáng đo không phải "nhanh bao nhiêu" mà "cái gì làm nó chậm đi".** Đo cold start một lần chỉ ra 8s. Đo **hai lần ở hai độ dài giấc ngủ khác nhau** (2′27″ và 10h38′) mới trả lời được câu quyết định thật: *có "ngủ sâu" không?* Không có ⇒ đánh đổi là hằng số, không phải hàm của thời gian bỏ bê. Cùng họ [[feedback-probe-by-difference]]: một phép đo cho số, hai phép đo cho **quan hệ**.
- **Trước khi liệt kê "đánh đổi", tách *tệ hơn phương án kia* khỏi *không tốt hơn phương án kia*.** Tôi xếp "snapshot bị huỷ mỗi lần deploy" vào cột nhược điểm của `suspend`; **chủ bác đúng**: `stop` thì lần nào cũng khởi động lạnh, nên deploy làm mất snapshot chỉ khiến `suspend` **bằng** `stop` ở lần đó, không tệ hơn. Một danh sách trade-off trộn hai loại này sẽ làm phương án không có đường thua trông như phương án rủi ro.
- **Proxy-based autoscaler mù với mọi thứ không phải request.** Fly nói thẳng: *"The proxy looks at inbound traffic. It does not look inside the container"* + *"no way for your application to tell the proxy 'I'm busy'"*. Hệ quả: request đồng bộ chạy 20s **an toàn** (proxy đếm connection đang mở), nhưng `BackgroundTasks`/`create_task` sinh ra **sau** khi response trả thì máy có thể bị ngắt giữa chừng, không log, không cảnh báo. Đây **không phải** Fly làm ẩu — đó là giới hạn trung thực của việc đo tải ở tầng proxy. ⇒ Chọn hạ tầng thì phải hỏi *"nó nhìn thấy cái gì"*, không chỉ *"nó làm được gì"*.
- **Một luật nền tảng thường có nhiều hệ quả, và người ta chỉ bắt được hệ quả nào cắn mình trước.** Luật GitHub: *workflow chỉ sống trên default branch*. §9 devops đã ghi nhận nó ngày 22/07 — nhưng **chỉ cho `workflow_dispatch`** (vì lệnh `gh workflow run` trả 404 ngay lập tức, đau ngay). Vế `schedule:` cũng chết vì đúng luật đó thì **không ai suy tiếp**, vì nó không đau — nó chỉ im lặng không chạy. ⇒ Bắt được một hệ quả của luật nào thì **đi liệt kê hết các hệ quả còn lại của chính luật đó** trước khi đóng mục.
- **Lịch chạy của app không được gắn vào hoạt động phát triển của repo** (bài của chủ, không phải của tôi). GitHub tắt scheduled workflow sau 60 ngày không commit trên default branch; mà `main` được §2.1 thiết kế để im lặng ⇒ **luật git của chính dự án bảo đảm cron sẽ chết**. Biến thể thứ 6 của [[feedback-gap-between-correct-decisions]], và là biến thể đầu tiên mà **hai quyết định đúng nằm ở hai TỔ CHỨC khác nhau** (luật của dự án × chính sách của GitHub) — không có file chung nào để mà tham chiếu chéo.
- **Công cụ sinh lại file cấu hình = máy xoá luật im lặng.** `fly launch` viết lại `fly.toml` và **xoá sạch** khối comment giải thích vì sao health check phải là `healthz` chứ không phải `readyz` — tức xoá đúng hàng rào chống tái phát sự cố Neon 21/07. Không diff nào đỏ (comment mà), không test nào bắt. Cùng họ với `.gitignore` neo path ở audit 02. ⇒ **Luật sống trong comment của file do công cụ sinh ra thì phải coi là luật tạm** — mỗi lần chạy công cụ đó, đi soi lại.
  - **📝 (bổ sung 24/07, chủ chỉ ra) Bắt được "tool xoá comment" mới là nửa bài; nửa còn lại: ĐỪNG để bản ghi bền sống trong file mà tool sở hữu.** Vá lần 23/07 của tôi là **chép comment TRỞ LẠI** vào `fly.toml` — tức đặt bản ghi vào đúng chỗ nó vừa bị xoá, chờ lần regen sau xoá tiếp. Lời giải đúng: lý do + giá trị nhạy sống ở `docs`/docstring (file `fly` không sinh lại), `fly.toml` chỉ giữ **một dòng trỏ**. Nguyên tắc tổng, dùng được cho mọi file bị công cụ quản (`fly.toml`, lockfile, `.terraform`, file scaffold): **thứ duy nhất được viết vào một file do công cụ sở hữu là thứ bạn chịu được mất.**
- **Hai "free tier" cùng tên nhà cung cấp là hai thứ khác nhau.** Free tier **Gemini API** mất khi bật billing trên project; free tier **Cloud Scheduler** tính theo billing account và **cần** billing mới dùng được. Một cảnh báo viết tắt (*"bật billing = mất free tier"*) đúng ở ngữ cảnh nó ra đời, rồi được đọc lại ở ngữ cảnh khác ⇒ suýt dẫn tới tách email/tách project vô ích. ⇒ Cảnh báo về free tier phải ghi kèm **free tier CỦA DỊCH VỤ NÀO**.
- **Một ngưỡng cảnh báo đúng tuyệt đối vẫn hoá mù khi giả định bên dưới nó đổi ở file khác.** `cost-brief` §7.2: *"Fly units/ngày ~86.400; >90.000 ⇒ đang chạy >1 máy"* — hoàn hảo khi máy always-on. Sau khi bật scale-to-zero, **86.400 chính là dấu hiệu hỏng** (máy đáng lẽ phải ngủ), mà ngưỡng cũ đọc nó thành "bình thường". ⇒ Đổi cấu hình runtime thì **đi soi lại mọi ngưỡng giám sát** — chúng không tự báo là đã hết đúng.

## ⏳ HÀNG ĐỢI HỌC — chưa học, chủ chủ động mở phiên (mở 2026-07-22)

Khác với phần trên: đây **không phải** bài đã rút ra, mà là **thứ đã chạm vào trong lúc làm và biết là mình chưa hiểu**. Ghi lại để nó không tan đi cùng phiên.

### D1 — Docker: layer cache và thứ tự các bước ⏳ mục tiêu: **trước 2026-07-25**

**Vì sao vào hàng đợi:** review PR #13 (008b) lộ ra một lỗi mà bản thân code hoàn toàn "đúng" — `ENV GIT_SHA` đặt ở tầng thứ hai của runtime stage. Docker build lại **mọi tầng nằm dưới tầng đầu tiên bị đổi**, mà mã commit thì đổi mỗi lần deploy theo định nghĩa, nên mỗi lần deploy cài lại toàn bộ dependency production chỉ để biết một chuỗi 40 ký tự. Dời một dòng xuống cuối: **32,8s → 2,5s** (đo thật trên máy chủ, mọi tầng `CACHED`, `readyz` vẫn trả đúng commit).

**Vì sao đáng học chứ không chỉ đáng vá:** đây là lớp lỗi **không test nào bắt được và CI luôn xanh** — cùng họ với sự cố Neon. Nó chỉ hiện ra khi biết cơ chế bên dưới. Từ 008b, deploy chạy mỗi lần merge nên mọi khoản lãng phí ở đây bị nhân lên theo nhịp làm việc.

**Vật liệu học đã có sẵn trong repo, không phải đi tìm:** `Dockerfile` (multi-stage thật, có cả stage Node bị vứt đi), commit `4a2062d` (chính bản vá + lý do), và một vòng lặp đo được bằng `docker build` hai lần.

**Phạm vi đề xuất cho phiên học (~1 buổi):** layer cache + thứ tự bước · vì sao multi-stage làm ảnh nhỏ đi · `.dockerignore` ảnh hưởng cache thế nào · vì sao `COPY` lockfile trước rồi mới `COPY` code. **Không** cần học: orchestration, compose, k8s — dự án chạy đúng một container.

### D2 — Thời gian **khởi động** của Python/FastAPI (khác trục với D1) ⏳ mở 2026-07-23, chưa hẹn ngày

**⚠️ Đọc D1 trước rồi đọc câu này: hai mục là HAI TRỤC KHÁC NHAU, đừng kỳ vọng mẹo của D1 chuyển sang được.** D1 là **thời gian build/deploy** (32,8s → 2,5s nhờ layer cache — máy CI đỡ mệt). D2 là **thời gian boot** — từ lúc `init` gọi `uvicorn` tới lúc `Application startup complete`. Không có layer cache nào giúp ở đây; nó chạy lại **mỗi lần máy dậy**.

**Vì sao vào hàng đợi đúng lúc này:** từ 23/07 app scale-to-zero ⇒ boot time chuyển từ *chuyện xảy ra khi deploy* thành **chuyện chủ cảm thấy mỗi lần mở app**. Đo thật: **~5 giây**, chiếm ~62% của 8s cold start (boot Firecracker chỉ 1,6s — phần đó Fly lo, không sửa được). Đây là **đòn bẩy trả một lần lãi mãi mãi**, khác với việc kéo dài thời gian máy thức (trả tiền tuyến tính).

**Một chỗ đã soi ra, nhưng ĐỪNG tưởng nó là 5 giây:** `Dockerfile` đặt `PYTHONDONTWRITEBYTECODE=1`, còn `UV_COMPILE_BYTECODE=1` **chỉ biên dịch dependency trong `.venv`**. Code của chính app ở `/app/backend/app/` không được compile lúc build, và cũng không cache được lúc chạy (rootfs mới tinh mỗi lần dậy) ⇒ compile lại mỗi lần boot rồi vứt. Vá bằng `RUN PYTHONDONTWRITEBYTECODE=0 python -m compileall -q /app/backend/app`. Nhưng ~25 file nhỏ ≈ **100–200ms**, không phải 5s — **nghi phạm chính là import graph** (SQLAlchemy/SQLModel + Pydantic v2 + Authlib trên 1 shared vCPU).

**Vòng lặp đo đã có sẵn, làm trước khi sửa bất cứ dòng nào** — đúng luật "đã-chạy ≠ chỉ-suy-luận":
```
flyctl ssh console -a microsched -C "python -X importtime -c 'import app.main'"
```

**Phạm vi đề xuất:** đọc `-X importtime` · import đắt vs import rẻ, lazy import có đáng không · `.pyc` sống ở đâu và khi nào mất · vì sao `suspend` **bỏ qua hẳn** khúc này (và vì thế D2 chỉ còn quan trọng cho lần dậy sau deploy / sau khi mất snapshot). **Không** cần học: gunicorn worker tuning, uvloop micro-benchmark — 1 user, 1 process.

## 2026-07-24 — 008a + 008d qua Agent-Opus: điều phối + hai bài vận hành

- **"Merge ≠ migration applied" — biến thể mới của gap-giữa-hai-thứ-đều-đúng.** CD deploy code lên Fly nhưng **không** chạy `alembic upgrade head`: không `release_command` trong `fly.toml`, không bước alembic trong `deploy.yml` — migration cố ý chạy tay qua `NEON_MIGRATOR_URL` (role không lên Fly). Merge PR migration xong, constraint **chưa** ở Neon cho tới khi có người chạy tay; không lỗi, không cảnh báo, PR đóng trông như xong. Cùng họ sự cố Neon: deploy đúng việc của deploy, migration đúng, lỗ nằm ở **khoảng trống giữa**. ⇒ Với mọi migration 009–012: áp tay lên Neon **rồi verify bằng query `pg_constraint` thật**, không dừng ở `alembic current = 0002` (current chỉ nói revision đã ghi, không nói constraint đúng nội dung).
- **Verify migration trên ĐÍCH THẬT, không chỉ trên container throwaway.** Agent chứng minh CHECK đúng trên `pgvector/pgvector:pg18` throwaway (tốt) + CI Migration QA xanh (tốt) — nhưng **cả hai không phải Neon**. Chỉ khi áp `0002` lên Neon rồi query `pg_constraint` trả đúng `ck_note_private_ciphertext` với đủ vế title+body NULL-tolerant thì mới là bằng chứng lỗ đã đóng **trên prod**. Cùng luật "đã-chạy ≠ chỉ-suy-luận", mở xuống tầng *chạy ở đâu*.
- **Điều phối Agent-Opus = vòng lặp biên lai chạy trơn khi bỏ chủ khỏi vòng đọc-báo-cáo.** T1 spawn Agent (worktree, nền, MAX) cho việc security-critical; Agent tự báo "5/5 xanh" **cả hai lần**, T1 vẫn đọc lại diff + checks + query DB từ đầu. Không phải nghi model — mà **biên lai là hạ tầng của tin cậy** (rủi ro thật đo được ở task 004: executor khai sai về chính việc vừa làm). Là mảnh còn thiếu để chủ lên vai điều-phối-của-điều-phối mà không mất lớp kiểm.
- **Graph chỉ đáng khi nó THẮNG text.** Over-graph báo cáo PR (vẽ scorecard cho danh sách nghiệm thu) — chủ chỉ ra đáng lẽ vài dòng. Graph để cho *mô hình/luồng/đánh đổi được nhìn*; danh sách "PR# + checks + điểm đã soi" thì text gọn đọc nhanh hơn. Luật report Hạng-1 = simplify **+ graph có điều kiện**, không phải graph mặc định.

## 2026-07-24 (cuối phiên) — dựng kênh `agy-bridge` (T1→T3) + probe năng lực model

- **Gap-giữa-hai-thứ-đều-đúng lại hiện, lần này giữa schema tool và CLI.** `agy-bridge` (MCP) chạy được nhưng auto-routing **im lặng rơi về "agy default"** (`no preferred model available`) vì tên model trong *schema* của bridge là tên đẹp `"Gemini 3.1 Pro (High)"`, còn `agy models` thật in `gemini-3.1-pro-high`. Hai bên đều "đúng" trong file của mình; chỗ khớp mới là chỗ hỏng — và nó **không báo lỗi**, chỉ lặng lẽ dùng model default (mất kiểm soát chất lượng). Đây là biến thể thứ N của [[feedback_gap_between_correct_decisions]] — vẫn: *không phải lỗi ở một bên, mà ở khoảng khớp giữa hai bên đều đúng*. Vá: luôn truyền `model:` bằng tên THẬT từ `agy models`.
- **Probe-by-difference áp cho việc CHỌN model, không chỉ đo bịa.** Trước khi giao model cho một lane, chấm nó bằng **input có đáp án biết trước**: plan login gài 2 lỗ critical (cache password plaintext + token vào localStorage) qua `adversarial_review`, và câu hỏi fact có đáp án trong `CLAUDE.md` qua `analyze_files`. Kết quả sạch: `gemini-3.6-flash-high` bắt đủ 2 critical + trả fact đúng + trích đúng dòng + **0 bịa**; `gemini-3.1-pro-high` vét thêm 9 lỗ thật (đủ cho review vét cạn). ⇒ Không chốt "model nào cho lane nào" bằng niềm tin/tên phiên bản (3.6 > 3.1 nhưng flash < pro — không suy ra được), mà bằng số đo. Ghi cứng kết quả + probe tái dùng ở memory `[[agy-model-capabilities]]`; model set đổi ⇒ chạy lại probe, đừng đoán.
- **Bất đối xứng chi phí lọc quyết định ưu tiên quota, không phải hạn mức.** 2 account Google AI Pro nuôi *cả* agy *lẫn* Jules. Chọn dồn vào agy (cố vấn có-T1-kiểm, lọc rẻ) thay vì Jules (workflow tự do, phải dựng lại ý định từ diff để lọc — gần bằng tự viết): **một nguồn chỉ đáng khi nó từ chối/kiểm được rẻ.** Cùng logic đã loại song-song-Jules ở `devops-brief.md` §7.f.

## 2026-07-24 (phiên tư vấn 008) — đặt khuôn, và phản biện khác-họ trước khi giao

- **Một bảo đảm "zero-trust" ở DB vẫn có lỗ dưới concurrency — "trigger gác" ≠ "không thể vỡ".** Chọn trigger cho bất biến `task_item` private⇒ciphertext vì nó "không vỡ âm thầm vì bug". Nhưng `adversarial_review` (Gemini pro, khác họ) chỉ ra: dưới READ COMMITTED, hai transaction đồng thời (toggle task→private ‖ thêm item trần) **không thấy việc chưa-commit của nhau** ⇒ cả hai qua trigger ⇒ item trần nằm trong DB của task private. Trigger là điều kiện **cần, không đủ** — phải `SELECT … FOR UPDATE` dòng cha ở tầng app để serialize. ⇒ Hỏi về một guard ở DB thì phải thêm *"dưới ghi đồng thời thì sao"*, không chỉ *"một luồng thì sao"*. Biến thể mới của [[feedback_gap_between_correct_decisions]]: khoảng trống giữa "trigger đúng" và "isolation level của Postgres".
- **Phản biện khác-họ lên SPEC (trước executor) bắt đúng lớp lỗi CI/test không thấy.** Chạy `adversarial_review` lên spec 008 → 7 finding, **0 bịa**, trích đúng dòng; 3 là lỗ thật. Đắt nhất là **mould-trap**: §2.2 "private thì mã hoá" đúng cho task/note, nhưng nếu 011 chép y hệt sang `tracker` (CHECK mã-hoá **vô điều kiện**) thì tạo tracker public = vỡ CHECK. Một spec đúng cho entity A thành bug khi chép sang entity B ràng buộc khác. ⇒ Task đặt khuôn **phải nêu rõ điểm biến thiên**, và **chạy review khác-họ lên spec trước khi giao** — lỗi khuôn nhân theo số slice chép nó. (Xác nhận sống đầu tiên của kênh `[[agy-model-capabilities]]` trên artifact thật, không phải probe.)
- **Trình bày finding phải dịch sang HẬU QUẢ, kể cả khi đang relay review kỹ thuật.** Trình 2 critical bằng nguyên văn tiếng reviewer (READ COMMITTED, `visible()`) → chủ không hiểu. Relay review là bẫy lapse riêng: dễ tưởng "review kỹ thuật thì nói tiếng kỹ thuật". Mỗi critical mở bằng *"hỏng chuyện gì của bạn"* + vẽ cơ chế (đua transaction chỉ sáng khi vẽ hai luồng cùng-lúc-cùng-lọt-cửa). Áp: [[feedback_explain_at_owner_level]].
