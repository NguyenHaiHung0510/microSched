 feat(tracker): tiến hoá mô hình tracker và cron reminder sang đa loại hình (031A)

 - Mở rộng CHECK constraint kind ('health', 'finance', 'general') trên tracker và tracker_group, bảo toàn composite FK (group_id, kind).
 - Thêm các cột reminder_mode, reminder_interval_days, reminder_action cùng các ràng buộc domain/DB tương ứng.
 - Cập nhật CronTimer hỗ trợ lịch fixed (bội số N ngày) và after_entry (dựa trên max(occurred_at) + N ngày).
 - Tích hợp generic Web Push payload routing (/reminder-confirm vs /trackers).
 - Migration 0011 với cơ chế expand/contract an toàn, backfill legacy thuốc và downgrade fail-closed.
 - Bổ sung bộ test unit/domain/migration/PG tương ứng.

 Co-Authored-By: Avicenna <agent-avicenna@microsched.local>
