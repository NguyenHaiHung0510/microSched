Task 012: normalize PostgreSQL 0009 constraint attestation

Parse pg_get_constraintdef prefixes before removing redundant parentheses so the pinned schema contract matches the exact migrated catalog. Include sanitized missing/extra constraint diagnostics and make RED/GREEN role cleanup revoke and drop owned privileges before dropping the synthetic role.

Co-Authored-By: Codex <noreply@openai.com>
