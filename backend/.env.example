# =============================================================================
# TÌNH TRẠNG CẤP PHÁT — soi thật 2026-07-22
# =============================================================================
# ✅ đã có · ⬜ chưa sinh · ⛔ cố ý KHÔNG cấp (không phải thiếu sót)
#
#   Biến                    | .env local | Fly secrets | Ghi chú
#   ------------------------|------------|-------------|---------------------------
#   DATABASE_URL            |     ✅     |     ✅      | role app, CRUD-only
#   NEON_MIGRATOR_URL       |     ✅     |     ⛔      | Alembic; chỉ chạy từ máy chủ
#   NEON_OWNER_URL          |     ✅     |     ⛔      | bootstrap; chỉ chạy từ máy chủ
#   GOOGLE_CLIENT_ID        |     ✅     |     ✅      |
#   GOOGLE_CLIENT_SECRET    |     ✅     |     ✅      |
#   ALLOWED_EMAILS          |     ✅     |     ✅      |
#   OAUTH_STATE_SECRET      |     ✅     |     ✅      | local và Fly để KHÁC giá trị
#   CRON_TOKEN              |     ✅     |     ✅      | local ≠ Fly. ⚠️ CÓ NƠI THỨ BA:
#                           |            |             | GitHub repo secrets, phải BẰNG
#                           |            |             | giá trị Fly — workflow cron gọi
#                           |            |             | production, không gọi local.
#   ENCRYPTION_MASTER_KEY   |     ✅     |     ✅      | sinh 2026-07-22; đã cất 3 nơi (§6 db-brief)
#
# Hai secret KHÔNG phải biến env của app — sống ở nơi khác, vẫn phải theo dõi ở
# đây vì đây là bảng duy nhất trả lời "cái gì đã cấp, ở đâu":
#
#   Secret                  | Ở đâu                | Ghi chú
#   ------------------------|----------------------|---------------------------
#   MICROSCHED_DEPLOY_TOKEN | GitHub repo secrets ✅| deploy token Fly, phạm vi CHỈ app
#                           |                      | microsched (không phải personal
#                           |                      | token, không phải org token) — tên
#                           |                      | phản ánh đúng phạm vi quyền.
#                           |                      | Đổi tên xong 2026-07-22, bản tên
#                           |                      | cũ đã xoá.
#                           |                      | ⛔ KHÔNG BAO GIỜ lên Fly secrets:
#                           |                      | app không gọi flyctl, nên đưa vào
#                           |                      | chỉ tạo đường leo thang "đọc được
#                           |                      | env" ⇒ "deploy được code tuỳ ý".
#                           |                      | Cùng lý do với hai dấu ⛔ ở trên.
#                           |                      | flyctl đọc biến FLY_API_TOKEN nên
#                           |                      | workflow map lại một dòng:
#                           |                      |   env: FLY_API_TOKEN:
#                           |                      |     ${{ secrets.MICROSCHED_DEPLOY_TOKEN }}
#   age private key         | 3 nơi ⬜ CHƯA SINH    | giải mã dump backup
#                           |                      | (db-and-data-model-brief §6).
#                           |                      | Public key sẽ nằm thẳng trong
#                           |                      | repo — an toàn theo thiết kế của
#                           |                      | age. Chưa cần: backup thật chưa
#                           |                      | dựng (008b chỉ có khung cron).
#
# Hai dấu ⛔ là **thiết kế, không phải việc còn thiếu**: chuỗi migrator/owner có
# quyền đổi schema và tạo role, nên chúng không bao giờ được rời máy chủ
# (`agent-tasks/006`, "cấm owner lên Fly"). Nếu một ngày thấy chúng xuất hiện
# trong `flyctl secrets list` thì đó là sự cố bảo mật, không phải tiện lợi.
#
# Soi lại bảng này bằng (không lệnh nào in ra giá trị thật):
#   flyctl secrets list --app microsched          # tên + digest trên production
#   grep -oE '^[A-Z_]+=' backend/.env | tr -d '=' # tên biến ở máy local
# =============================================================================


# Fly secret: runtime role microsched_app (CRUD only; never use an owner URL here).
DATABASE_URL=postgresql://microsched_app:password@host/database?sslmode=require

# Local only: schema owner used by Alembic; never upload this URL to Fly.
NEON_MIGRATOR_URL=postgresql://microsched_migrator:password@host/database?sslmode=require

# Local bootstrap only: Neon owner; never upload this URL to Fly or runtime code.
NEON_OWNER_URL=postgresql://neondb_owner:password@host/database?sslmode=require


# --- Auth (007) ---
# OAuth client "microSched web" (Google Cloud Console, External/Testing).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Danh sách email được phép đăng nhập, phân tách bằng dấu phẩy, so sánh lowercase.
ALLOWED_EMAILS=
# CHỈ để ký cookie state/nonce tạm trong lúc handshake OAuth — KHÔNG phải session
# đăng nhập (session = opaque token + bảng `session`, auth-brief §2).
# Sinh: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Local và Fly nên để hai giá trị KHÁC nhau.
OAUTH_STATE_SECRET=

# --- Cron (008b) ---
# Bearer secret cho các endpoint do GitHub Actions gọi (backup / embed / nhắc
# thuốc). KHÔNG đi qua session user — auth-brief §5.
# Sinh: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Local và Fly để hai giá trị KHÁC nhau (cùng lý do với OAUTH_STATE_SECRET).
CRON_TOKEN=

# --- Mã hóa cột (008a) ---
# Master key AES-GCM cho các cột 🔐 (schema-physical-brief §7.1 + K18, prefix enc:v1:).
# Sinh: python -c "import base64,os; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
#
# ⚠️⚠️ MẤT KHÓA NÀY = MẤT VĨNH VIỄN toàn bộ tracker/subscription/entry + mọi
# note/task private. Không có đường khôi phục — không backdoor, không reset, không
# hỗ trợ nhà cung cấp. Neon giữ ciphertext; khóa chỉ nằm ở nơi BẠN cất.
# Trước khi ghi dòng dữ liệu mã hóa ĐẦU TIÊN, cất khóa theo đúng
# db-and-data-model-brief.md §6 — BA nơi phân vai:
#   1. password manager  (bản chính, tra cứu nhanh)
#   2. note trên điện thoại  (dự phòng tiện tay)
#   3. USB offline  (last-resort, không cắm thường xuyên)
# Không nơi nào trùng domain với Google Drive (nơi giữ dump) — mất Drive không
# kéo theo mất khóa. Mất CẢ BA mới mất khả năng giải mã.
# Fly secrets KHÔNG phải bản backup — đó là bản đang chạy.
ENCRYPTION_MASTER_KEY=

# --- Deploy (008b) — KHÔNG phải biến env của app ---
# Token deploy Fly, dạng "FlyV1 ...". Sống ở GitHub repo secrets, không bao giờ ở
# file này và không bao giờ ở Fly secrets. Tên đặt theo PHẠM VI QUYỀN của nó:
# token này chỉ có quyền trên app `microsched`, không phải personal/org token —
# nên gọi nó FLY_API_TOKEN sẽ ngụ ý rộng hơn quyền nó thật sự có.
# Đổi lại, flyctl đọc mặc định biến FLY_API_TOKEN ⇒ workflow map lại một dòng.
# Không đặt giá trị vào đây kể cả để thử — xem bảng cấp phát ở đầu file.
MICROSCHED_DEPLOY_TOKEN=
