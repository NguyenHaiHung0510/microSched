## 2024-05-24 - Tooltips for Icon-Only Buttons
**Learning:** Icon-only buttons with `aria-label` are accessible to screen readers, but sighted users may still find them confusing without tooltips. Using `Tooltip` components wrapping icon buttons greatly enhances usability.
**Action:** Always wrap icon-only buttons with `Tooltip` (alongside `aria-label`) when space allows.
